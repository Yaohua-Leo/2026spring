Week 1
Part 1 Review and study basic conopts \＆basic theory by studying important classes of gps．
Part 2 Group actions．
Permutation groups－ $0^{\prime}$ Nan－Scott thm
Part 3 Linear gps－classical groups \＆subgraps．

Cyclic graps（denoted by $\mathrm{Cn}_{n}$ ）
$G=\langle g\rangle=\left\{1, g, g^{2}, g^{3}, \ldots\right\}$（or $\{0,1,2, \ldots\}$ sometimes written additively since commutative）
－Cyclic groups are uniquely
determined by the orders．
－subgraps are cyclic
－factor graps are cyclic
＊Aut（ $\left.C_{n}\right) \quad\left(C_{n}=\langle g\rangle\right)$
prop Aut $\left(C_{n}\right) \simeq(\mathbb{Z} / n \mathbb{Z})^{\bar{x}}=\{\bar{x} \mid(x, n)=1\}=U(n)$ ． pf：$\forall 6 \in \operatorname{Aut}(C n), 6(g)=g^{i}$ for some $i \in \mathbb{Z}$ and the integer i uniquely determines 6 ．Denote this automorphism by 6i．We have $(i, n)=1$ since $g^{i}$ must have the same order as $g$ ．On the other hand，$\forall a$ s．t．$(a, n)=1$ ，the map $g \mapsto g^{a}$ is an antomorphism of $C_{n}$ ．Hence we have a surfective map

$$
\begin{aligned}
\psi: \operatorname{Aut} 1\left(C_{n}\right) & \rightarrow(\mathbb{Z} / n \mathbb{Z})^{x} \\
6 a & \mapsto a(\bmod n)
\end{aligned}
$$

$\psi$ is obviasly a homo and injective．

- Aut $\left(C_{p}\right)=C_{p-1}=\langle 6\rangle$
luse the fact that $\mathbb{F}_{p}{ }^{x}$ is cyclic)

Recall let $F=G F\left(p^{l}\right)$
$F^{+}=C_{p}^{\lambda}=C_{p} \oplus \cdots \oplus \varphi$
$F^{x}=C_{p}{ }^{e}-1$

D Aut $\left(C p^{e}\right)=C p^{e-1}(p-1)$ where $e \geqslant 2, p$ is odd prime. Pf $1: r=\lambda$ or $p+\lambda$ is a primitive rout for $C_{p}{ }^{e}$ where orderp $(\lambda)=p-1 (p+\lambda, p)=1 \Rightarrow\left(p+\lambda, p^{e}\right)=1 \Rightarrow(p+\lambda)^{p^{e-1}}(p-1) \equiv 1\left(\bmod p^{e}\right)$ It suffices to show $p^{e-1}(p-1)$ is the smallest integer $n$ s.t. $(p+\lambda)^{n} \equiv 1\left(\bmod p^{e}\right)$ or $\lambda^{n} \equiv 1\left(\bmod p^{e}\right)$.
First note that $p-1 \mid \operatorname{arder} p e(r)$ since $r^{|r|} \equiv 1(\bmod )^{p}$ yields $\quad r^{|r|} \equiv 1\left(\bmod \operatorname{pe}^{p}\right)^{p-1}$ so $p-1|r|$. Then it suffices to show $(r)^{p^{e-2}}(p-1) \neq 1\left(m_{0} d p^{e}\right)$.
When $e=2, \quad(p+\lambda)^{p-1} \equiv \lambda^{p-1}+p(p-1) \lambda^{p-2}\left(\bmod p^{2}\right)$
Here we use a lemma: $\forall \lambda$ primitive rot of $C_{p}$, either $\lambda$ or $p+\lambda$ is a primitive rout for $\left(\mathbb{Z} / p^{2} \bar{Z}\right)^{x}$.
By above, $(p-1)||\lambda|$, so $| \lambda \mid=p-1$ or $p(p-1)$. If $|\lambda|=p-1$, then $(p+\lambda)^{p-1} \equiv \lambda^{p-1}+p(p-1) \lambda^{p-2}\left(\bmod p^{2}\right) \equiv \mid p(p-1) \lambda^{p-2}(\bmod f$ Then since $p \nmid \lambda^{p-2},(p+\lambda)^{p-1} \neq 1\left(\bmod p^{2}\right)$, so $p+\lambda$ is a primitive rot for $\left(Z / p^{2} Z l\right)^{X}$.
Suppose it works for $e \leq n$. ie. $r^{p^{n-k}(p-1)} \not \equiv 1\left(\bmod p^{n}\right)$.
Since $\quad r^{p^{n-2}(p-1)} \equiv 1\left(\bmod p^{n-1}\right)$, we have $\quad r p^{n-1}(p-1)=1+d p^{n-1} 0$ for some $(d, p)=1$. Then $r p^{n-1}(p-1)=\left(1+d p^{n-1}\right)^{p} \equiv 1+d p^{n}+\binom{p}{2} d^{2} p^{2 n-2}\left(\bmod p^{n+1}\right) \equiv 1+d p^{n} \neq 1\left(\bmod p^{n+1}\right) \circ$
Remark: We actually proved that if $r$ is a primitive root fir $\left(\mathbb{Z} / p^{2} \mathbb{Z}\right)^{x}$, Then $r$ is a primitive rot for $\left(\mathbb{Z} / p^{k} \mathbb{Z}\right)^{x}$ for any $k$. And we prove existence of such $r$ by noting that if $\lambda$ is a primiture root for $C_{p}$, then $\lambda$ or $p+\lambda$ is a primitive not of $\left(\mathbb{P} / p^{2}\right)$ C
Compus
of 2: prove that each Sylow q-gp is cyclic, \& prime and each finite abelian group is a direct sum: of cyclic group (1) Sylow $p$-subgranp is cyclic

Claim: 1+p is an element of order $p^{n-1} n_{\alpha-2}\left(\mathbb{Z} p^{\alpha} \mathbb{Z}^{x}\right)^{x}$ (Show that $(1+p)^{p-1} \equiv 1\left(\bmod p^{\alpha}\right)$ but $(1+p)^{p^{\alpha-2}} \neq 1\left(\bmod p^{\alpha}\right)$. (2) Sylow q-subgrap is cyclic, q $\neq p$.

The map

$$
\begin{aligned}
\mathbb{Z} / p^{\alpha} \mathbb{Z} & \rightarrow \mathbb{Z} / p \mathbb{Z} \\
a+\left(p^{\alpha}\right) & \mapsto a+(p)
\end{aligned}
$$

is a ring homo which gives a surjective homo from $\left(\mathbb{Z} / p^{\alpha} \mathbb{Z}^{x}\right.$ onto $(\mathbb{Z} / p \mathbb{Z})^{x}$. The latter group is cyclic of order $p-1$. The kernel of this map is of order $p^{\alpha-1}$, hence for all primes $q \neq p$, the sylow $q$-subgrap of $\left(\mathbb{Z} / p^{\alpha} \mathbb{Z}\right)^{x}$ maps isomophically into the cyclic group $(\mathbb{Z} / p \mathbb{Z})^{X}$. All Sylow subgraps of $\left(\mathbb{Z} / p^{\alpha} \mathbb{Z}\right)^{x}$ are therefore cyclic, so $\left(\mathbb{Z} / p^{\alpha} \mathbb{Z}\right)^{x}$ i? cyclic.

$$
\begin{equation*}
\operatorname{Aut}\left(C_{2}^{e}\right)=C_{2}^{e-2} \times C_{2} . \quad(e \geqslant 2) \tag{.$\quad \alpha z!$}
\end{equation*}
$$

Note that $C_{2}$ and $\mathbb{Z} / \mathbb{Z}$ have primitive roots $\left(1\right.$, and 3 respectively), but not for $\mathbb{Z} 2^{\alpha}$ (1) If $a=2 b+1$ for some $b, k \geqslant 3$, then $a^{2^{k-2}} \equiv\left(\bmod 2^{k}\right.$ when $k=3 \quad a^{2}=(2 b+1)^{2}=4 b^{2}+4 b+1=1+4 b(b+1)\left(\bmod z^{3}\right)$
Since $814 b(b+1), a^{2} \equiv 1\left(\bmod 2^{3}\right)$.
Suppose it holds, for $k \leqslant n$.
Then $\left(1+\ell \cdot 2^{k}\right)^{2}=\ell^{2} \cdot 2^{k+1}+\ell \cdot 2^{k+1}+1 \equiv 1\left(\bmod 2^{k+1}\right)$.
(2) 5 is an element of order $2^{k-2}$ in $\mathbb{Z} / 2^{k} \mathbb{Z}_{k-3}^{X}$ (shaw that $\left(1+2^{2}\right) 2^{k-2} \equiv$ (mod $\left.2^{k}\right)$ and $\left(1+2^{2}\right)^{k-3} \equiv 1+2^{k-1}$ 丰
(3) Since $\left(\mathbb{Z} / 2^{k} \mathbb{Z}\right)^{x}$ is finite abelian, it is a direct product $O$ of cyclic graps: It has an element of order $2^{k-2}$, then $\left(\mathbb{Z} / 2^{k} \mathbb{Z}\right)^{X} \simeq C_{2^{k-2}} \times C_{2}$.
$D$ Aut $\left(C_{n}\right)=$ Aut $\left(C_{p_{1}} e_{1}\right) \times \cdots \times$ Aut $\left(C_{p_{t}}{ }^{e t}\right)$ where $n=p_{1}^{e_{1}} p_{2} e_{2} O_{1} p_{t}$ pf: Corollary of Chinese Remainder Theorem.
Thim (Chinese Remainder Theorem) $\quad v$ commutative with identity). ◯ let $A_{1}, A_{2}, \cdots, A_{k}$ be ideals in: $R^{V}$. The map $\left(\mathbb{Z} / 2^{k} \mathbb{Z}\right)^{X} \simeq C_{2} k-2 \times C_{2}$.
◯ $R \xrightarrow{\varphi} R / A_{1} \times R / A_{2} \times \cdots \times R / A_{k}$
defined by $\quad r \mapsto\left(r+A_{1}, r+A_{2}, \cdots, r+A_{k}\right)$ is a ring homo with kernel $A_{1} \cap A_{2} \cap \ldots / A_{k}$.

If for each $1, j \in\{1,2, \cdots, k\}$ with $1 \neq j$ the ideals $A_{i}$ and.
$A_{j}$ are coprime $\left(A_{i}+A_{j}=R\right)$ then this map is surjective ◯ and $A_{i} \cap A_{j} \cap \cdots \cap A_{k}=A_{1} \cdots A_{k}$ so

$$
R /\left(A_{1} A_{2} \cdots A_{k}\right)=R /\left(A_{1} \cap A_{2} \cap \cdots \cap A_{k}\right) \simeq \prod_{i=1}^{k} R / A_{i}
$$

◯ ◯
$\_\_\_\_$
or simply
Thm (CRT)
Let $m_{1}, \cdots, m_{n}$ be pairwise coprime integers. Then ◯

$$
\mathbb{Z} /\left(m_{1} \cdots m_{n}\right) \mathbb{Z} \simeq\left(\mathbb{Z} / m_{1} \mathbb{Z}\right) \times \cdots \times\left(\mathbb{Z} / m_{n} \mathbb{Z}\right)
$$

$\_\_\_\_$
of of general version: ◯
(1) homo with $\operatorname{ker} \varphi=A_{1} \cap \cdots \cap A_{n}$ is obvians
◯
(2) surjective if coprime
lemma $\cdot R$ is a ring with identity. $I_{1}, I_{2}, J$ are ideals of $R$. If $I_{i}$ and $J$ are coprime, then $I_{1} I_{2}$ and $J$ are coprime.
prampus

$$
\begin{array}{llll}
\exists a_{1} \in I_{1}, & b_{1} \in J & \text { s.t. } & a_{1}+b_{1}=1 \\
\exists a_{2} \in I_{2}, & b_{2} \in J & \text { s.t. } & a_{2}+b_{2}=1
\end{array}
$$

◯

Then $\left(a_{1}+b_{1}\right)\left(a_{2}+b_{2}\right)=1=\underbrace{a_{1} a_{2}}_{\in I_{1} I_{2}}+\underbrace{a_{1} b_{2}+b_{1} a_{2}+b_{1} b_{2}}_{\in J}$
By lemma, Ir and Ir... In are coprime, so $\exists a \mid \in I_{1}, b \in I_{2} \cdots I_{n}$ s.t. $a_{1}+b=1$
let $x_{1}=1-a 1=b$, then $x_{1}+I_{1}=1-a_{1}+I_{1}=1+I_{1}$

$$
x_{1}+l_{i}=b+l_{i}=l_{i}, i \neq 1
$$

$\Rightarrow \varphi\left(X_{1}\right)=\left(1+I_{1}, I_{2}, \cdots, I_{n}\right)$
Similarly find $x_{i}$ and so $\forall x=\left(r_{1}+I_{1}, \cdots, r_{n}+I_{n}\right) \in \prod_{i=1}^{k} R / A \exists r=r_{1} x_{1}+\cdots+r_{n} x_{n}$ s.t. $x=\varphi(r)$.
(3) coprime then $A_{1} \cap \cdots \cap A_{n}=A_{1} \cdots A_{n}$
$\underline{2}$ is obvials.
∴ By induction, it suffices to prove $A_{1} \cap A_{2} \leqslant A_{1} A_{2}$. Indeed, if $a \in A_{1} \cap A_{2}, \ldots x \in A_{1}, y \in A_{2}$ are such that
$x+y=1$. then $a=a(x+y)=\underset{1}{a}+\underset{1}{a}+\underset{1}{a} \in A_{1} A_{2}$ since $R$
is commutative.
Since the isomorphism in the Chinese Remainder Theorem is an isomorphism of rings, in particular the groups of units on both sides must be isomorphic. Then

$$
(\mathbb{Z} / m n \mathbb{Z})^{x} \simeq(\mathbb{Z} / m \mathbb{Z})^{x} \times(\mathbb{Z} / n \mathbb{Z})^{x}
$$

wherever $(m, n)=1$.
Remark: 3 elements of order 2 in $\operatorname{Aut}\left(\dot{C}_{2}{ }^{e}\right)$
6. $g \rightarrow e^{-3} g^{-1}$
$z_{0} \approx z^{2^{e-3}}: g \rightarrow g^{2^{e-1}+1}$. e.g. use $z_{\xi} g \rightarrow g^{5}$
$6 \tau_{0}=6 r^{2^{e-3}}=g \rightarrow g^{2^{e-1}-1} \quad\left(=\left(g^{2^{e-1}+1}\right)^{-1}\right)$

## Dihedral grap $G=D_{2 n} \quad$ 二面体群

$G=\langle a, b\rangle, \quad a^{2}=b^{2}=1$

## Ex

Let $a=\left(\begin{array}{cc}0 & 1 \\ 1 & 0\end{array}\right), \quad b=\left(\begin{array}{cc}0 & 2 \\ \frac{1}{2} & 0\end{array}\right) \in G L(2, \mathbb{R})$ ．
$|a|=|b|=2, a b=\left(\begin{array}{cc}\frac{1}{2} & 0 \\ 0 & 2\end{array}\right) \rightarrow$ elements of order 2
$G=\langle a, b\rangle$ is dihedral，infinite．
$>$ Then $|G|$ finite $\Leftrightarrow$ labl finite
Celements of 6 are $a, a b, a b a, a b a b, \cdots$ and $b, b a$ ， bab，baba，．．．as soon as we know the first term in these lists to give the identity element，we know．$G$ ．It $\mathrm{can}^{2}$＇t be elements like $a b a b a$ ，since $1=a b a b a=b a b=a$ so if must be $(a b)^{m}$ for some $m$ then $G$ is finite． （note that $(a b)^{m}=1 \Leftrightarrow(b a)^{m}=1$ ）．

$$
>G=\left\{(a b)^{i},(b a)^{j},(a b)^{i} a,(b a)^{j} b\right\}
$$

$$
\Rightarrow G=\left\{(a b)^{\top}, b(a b)^{\top} \mid i\right\} .
$$

Since $(b a)^{j}=(b a)^{j} b^{2}=b(a b)^{j} b=\left((a b)^{j}\right)^{-1}$
◯
◯ ）
◯

$$
(a b)^{1} a=b(b a)^{1+1}=b\left((a b)^{\top}\right)^{-1}
$$

$\_\_\_\_$
then if 6 is finite，$|a b|=n$ ，so $|6|=2 n$ and $|a b|=\frac{|6|}{2}$ D $6=\langle a b, b\rangle=\langle a b, a\rangle=\langle a b\rangle:\langle b\rangle=C_{n}: C_{2}$

| semidirect product |
| :--- |
| $\qquad \|$def let 6 be a gp，$A, B<C$ <br> （1）$A \triangleleft 6$（2）$A \cap B=1$ <br> （3）$G=A B$ |
| Campus |
| Then 6 is called a $A$ by |
| semidrect，product sented $A=B$ or $A \triangle B$ |

D $\operatorname{Aut}\left(\dot{D}_{2 n}\right)_{<z=g \rightarrow g},\left\{\sigma_{i} \mid \operatorname{Gi}_{i}: g \rightarrow g^{i},(\{, n)=1\}\right.$
Conver) $n$ is odd $\uparrow^{b \rightarrow g b} \uparrow \quad\left(6 \in \operatorname{Ant}\left(D_{2 n}\right)\right.$ :
Cover $\operatorname{Aut}\left(D_{2 n}\right) \simeq C_{n}: \operatorname{Aut}\left(C_{n}\right)$
$6: g \rightarrow g^{i},(i, n)=1$
In particular, Aut $\left(D_{2 p}\right)=C_{p}: C_{p}-1$
$b \rightarrow g^{\prime} b \quad 0 \leq j \leq n$
Aut $\left(D_{2 p^{l}}\right)=C_{p}{ }^{e}: C_{p}{ }^{e-1}(p-1)$
$\left|A_{n}+\left(D_{2 n}\right)\right|=n \phi(r$
Hint: $G=D_{2 n}=\langle g\rangle:\langle b\rangle=C_{n}: C_{2}$
If $6 \in$ Aut $(6)$, then $g^{6}=g^{i}$ since $\cdot\left(g^{6}\right)$ is odd
Conjugacy classes of elts of $D_{2 n}=\langle g\rangle:\langle b\rangle$
(I) $n$ is odd

$$
=\left\{g^{i}, g^{i} b \mid 0 \leqslant i, j \leqslant n-1\right\}
$$

(i) elts of odd order form I all of order 2 conjugacy classes $5 g^{i}, g^{-i} 3$
(ii) elts of even order are all conjugate.

C $p f:$
Claim: $g^{j} b$ and $g^{j+1} b$ are conjugate.
Observe that
(1) D2n has exactly $n$ elts of order $2: g^{j} b, 0 \leq j \leq n$ (2) $g^{-i} b g^{i}$ with $0 \leqslant i \leqslant n-1$ are of order 2 and distine and are obviansly conjugated $\Rightarrow g^{i} b=g^{-j} b g^{\prime}$ for some $j$ and gib are unjugated
(2) $n$ is even.

## Week 2

Constructing aps and finding Aut(6) for the new (6).

## Construction

Let $\sigma$ be a group and let $H \leqslant \operatorname{Aut}(6)$. Let $X=\{g h \mid g \in 6$, $h \in H\}$ s.t. $\left(g_{1} h_{1}\right)\left(g_{2} h_{2}\right)=\left(g_{1} g_{2} h_{1}^{-1}\right)\left(h_{1} h_{2}\right)$. Then $X$ is a gp Denoted by G.H in which G aX and $X / G \simeq H$.

Example cyclic group

Let $G=Z_{7}=\langle g\rangle$. Then $\operatorname{Aut}(G)=Z_{6}$.
(1) Let $6 \in \operatorname{Aut}(6)$ of order $2 \quad\left(g^{6}=g^{-1}\right)$

Then $x=6 \cdot\langle 6\rangle=\langle g\rangle \cdot\langle 6\rangle$ with $(g 6)(g 6)=g g^{6}=1$
$\left.\Rightarrow X=\langle g, 6| g^{6}=g^{-1},|g|=7,|6|=2\right\rangle=Z_{7} \cdot Z_{2}=D_{14}$.
(2) Let $z \in \operatorname{Aut}(6)$ of order 3

Then $g^{2}=g^{2}$
$\Rightarrow x=\langle g, 2\rangle$ where $|g|=7,|z|=3, g^{2}=g^{(2)}=z_{7}: z_{3}$

Example general cyclic grap.
Let $G=\langle g\rangle=Z_{p} . p$ prime. Then $\operatorname{Aut}(6)=Z_{p}-1$

Let $6 \in \operatorname{Aut}(6)$ s.t. $|6|=l$. Then $l \mid(p-1)$ and $\langle 6\rangle=Z l$. $X=6 \cdot\langle 6\rangle=Z_{p} \cdot Z_{l}=\langle g, 6\rangle$ where $|g|=p,|6|=l, g^{6}=g^{\lambda} O$ If $|6|=2$, then $X=D_{2 p}$
$\left(Z_{7}: Z_{6}=Z_{7} \times Z_{6}\right.$

If $l=p-1$, then $x=2 p=2 p-1$
$X=$ AGL $(1, p) \Leftrightarrow Z(x)=1$
$\left(Z_{7}: Z_{3}\right) \times Z_{2}$
$\left(Z_{7} \cdot Z_{2}\right) \times Z_{3}$
$z_{7}: z_{6}$

10 O

In general, $G=Z n, X=Z n \cdot H, H \leqslant U(n)$.
Campus

## Example

$$
6=Z_{35}=Z_{5} \times Z_{7} . \quad H \leqslant A_{4}+(6)=Z_{4} \times Z_{6}=\left\langle b_{1}\right\rangle \times\left\langle b_{2}\right.
$$

$X=Z_{35}: H<\left(Z_{5}: Z_{4}\right) \times\left(Z_{7}: Z_{6}\right)$
$X_{0}=Z_{35}:\left\langle 61^{2} 62^{3}\right\rangle=D_{70}$
Let $G=D_{2 n}=Z_{n}: Z_{2}$
Then $A_{n}+(6)=Z_{n}: U(n)=Z_{n}: A_{n}+\left(Z_{n}\right)$.

## Example finite field.

Let $F=G F\left(p^{f}\right)$ and let $G=F^{+} \simeq Z p^{f}, H=F^{x} \simeq Z p_{-1}^{f}$ Pefine a $\operatorname{grap}_{h_{1}} X=\{g h \mid g \in G, h \in H\}$ s.t. $\left(g_{1} h_{1}\right)\left(g_{2} h_{2}\right)=g_{1} g_{2}^{h_{1}^{-1}} h_{1}$ where $g_{2} h_{1}^{-1}=g_{2} h_{1}^{-1}, n F$

## ↓ multiplication.

Let $g, h \in F$. Then $h$ is an anto of $G=F^{+}$induced by multiplication by $h$. as $(a+b)^{h}=(a+b) h=a h+b h=a^{h}+b^{h} \Rightarrow x=6: H=Z_{p} f: Z_{p}^{f}-1$ "singer cycle"
Aut $(x)=X:\langle\tau\rangle=\left(Z_{p}^{f}: Z_{p}^{f-1}\right): Z_{f}=\left(Z_{p}^{f}:\langle g\rangle\right):\langle\tau:$ where $z=g \mapsto g^{p}$ "Frobenius" anto个?

In parficular, $A_{u} t\left(Z_{p}: Z_{p-1}\right) \simeq Z_{p}: Z_{p-1}$

## Example

$$
\text { Assume } p=3, f=2
$$

Then $\operatorname{Aut}(X)=\left(Z_{3}^{2}: Z_{8}\right): Z_{2}$ where $z: g \mapsto g^{3}$

$$
\downarrow\langle g\rangle \downarrow\langle 2\rangle \quad=\langle a, b| a^{4}=1, a^{2}=b^{2}, a^{b}=-a
$$

Ret $a=g^{2} \& b=g 2$. Let $\langle a, b\rangle=Q_{8}=\{1,-1,1,-1, j,-j, k,-1$
$\left(b^{2}=g z g z=g g^{-1} z^{2}=g^{4}\right.$
$|b|=4$

$$
a^{2}=b^{2}
$$

$$
a^{b}=(g z)^{-1} g^{2} g z=g^{6}=\left(g^{2}\right)^{-1}=a^{-1}
$$

$b^{a}=b^{-1}$
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-09.jpg?height=178&width=232&top_left_y=2238&top_left_x=1527)

Date <g>: <<>> < $\left\langle g^{2}, g z\right\rangle$

Ant $(X)=\left(Z_{3}^{2}: Z_{8}\right): Z_{2}=Z_{3}^{2}:\left(Z_{8}: Z_{2}\right)>Z_{3}^{2}: Q_{8}$

HW: all elts of $Z_{3}^{2}=Q_{8}$ of order 3 are conjugate.

## Group action

Def group action
let 6 be a grapp and $\Omega$ a set. We say that 6 acts on $\Omega$ if
(1) each elt $g$ of $G$ is a $1-1$ map from $\Omega$ to $\Omega$, the image of $\omega \in \Omega$ under $g$ is denoted by $\omega^{g}$
(2) $w^{\prime}=w \quad \forall w \in \Omega$.
(3) $\left(w^{g_{1}}\right)^{g_{2}}=w^{g_{1} g_{2}}$
◯

## Example

anjugacy class of $g$
(1) $G L(\Omega)$ acts on $\Omega=F^{n}$
(2) conjugation $\quad \Omega=0 . \quad w^{g}=g^{-1} w g . \quad C(g)=\left\{g^{x} \mid x \in G\right\}$
(3) right multiplication $\Omega=0 \quad \omega^{9}=\omega g$.

Lemma $\quad(N / C$-lemma)

For $H<G$, we have $C_{G}(H) \nabla N G(H)$ and $\operatorname{Ng}(H) / C_{G}(H) \lesssim \operatorname{Aut}(H)$.

In particular, if $H \& G, G / C_{G}(H) \lesssim A u+(H)$.
pf: $\quad N 6(H)$ acts on $H$ by conj. with kernel equal to $C_{6}(H)$.

Campus

Week 3
Group actions
6 finite.
Thm (Cauchy)

## If $p||G|$, then $\exists g \in G$ s.t. $| g \mid k p$.

let $G$ act on a set $\Omega$. Then $G$ partitions $\Omega$ into orbits:

$$
\Omega=\Omega_{1} \dot{U} \Omega_{2} \dot{U} \cdots \dot{U} \Omega_{m}
$$

Timm Corbit - stabilizer thm)

$$
|G|=|\Omega i|\left|G_{w i}\right| \text { where } w_{i} \in \Omega_{i} \text { and } G_{w i}=\left\{g \in G \mid w_{i}^{g}=w_{i}\right.
$$

Example conjugation action.
Assume $\Omega=6$ and 6 acts on $\Omega$ by conjugation. Ther $G=\{1\} \cup C\left(x_{1}\right) \cup \cdots \cup C\left(x_{t}\right)$
Then $|G|=\left|C\left(x_{i}\right) \| C_{G}\left(x_{i}\right)\right|$. In particular, $\left|C\left(x_{i}\right)\right|||G|$
Thm If $|G|=p^{e}$ with $p$ prime, then $z(G) \neq 1$.
Pf: $G=\{1\} \dot{U} C\left(X_{1}\right) \dot{U} \cdots \dot{U} C\left(X_{t}\right)$
Then $|G|=p^{e}=\left|+\left|C\left(x_{1}\right)\right|+\cdots+\left|C\left(x_{t}\right)\right|\right.$

$$
=1+p^{e_{1}}+\cdots+p^{e_{t}} \quad \text { where } e_{i} \geqslant 0 \text {. }
$$

It follows that $p^{e i}=1$ for some $i$. So $\left|k\left(x_{i}\right)\right|=1 \Rightarrow \mid \neq x_{i} \in Z(6$
Remark: If $x$ and $y$ are conjugate, then $|x|=|y|$.
Converse is not necessarily correct. i.e. In $C_{m}=\langle g\rangle$. $g$ and $g^{-1}$ are not conjugate.

Problem If 6 is a grap satisfying that elts of the same order 198 are conjugate, then $G=S_{2}$ or $S_{3}$. (solved independently by " W. Fit-GP.
$3 *$ 继平

Sylow thms $G$ finite, $|G|=p^{e} m$ with $p$ prime and $\operatorname{gcd}(p, m)=1$ Def Sylow $p$-subgroup

If 6 has a subyp $H$ s.t. $(H)=p^{e}$, then $H$ is called a Sylow p-Subgrapp.

Remark existence of $H \Rightarrow$ Canchy thm.
Thm (first Sylow thm)
Sylow p-subgraps exists $\forall p||6|$.
$p f$ : use conjugation equation.

$$
\begin{aligned}
& G=\operatorname{Si} 3 \dot{U} C\left(X_{1}\right) \dot{U} U \cdots \dot{U} C\left(X_{t}\right) \\
& |G|=1+\left|C\left(X_{1}\right)\right|+\cdots+\left|C\left(X_{t}\right)\right|=Z(6)+\left|C\left(X_{1}\right)\right|+\cdots+\left|C\left(X_{s}\right)\right|
\end{aligned}
$$

(1) $p X z(6)$.

Then $\exists C\left(x_{i}\right)$ s.t. $\left(C\left(x_{i}\right), p\right)=1$ and $C\left(x_{i}\right) \neq 1$.
By orbit - stab thm, as $|6|=\left|C\left(x_{i}\right)\right|\left|C_{6}\left(x_{i}\right)\right|, p^{e} \|\left|K_{6}\left(x_{i}\right)\right|$ i.e. $C_{\sigma}\left(X_{i}\right)$ is a subgrap of $G$ of order divisible by $p^{e}$ with $K_{G}\left(x_{i}\right)|<|6|$.
By induction, $C_{6}\left(x_{i}\right)$ has a sylow $p$-subgraup $P$. Then $P$ is a Sylow $p$-subgranp of $G$.
(2) $p \mid z(0)$.
$z(6)$ is an abelian granp. We first prove, Cauchy's thm for abelian grap.
Let $G$ be abelian. $r^{p}$ pick $\mid \neq x \in G$. If $p\left||x|\right.$, then. $x^{\frac{|x|}{p}}$ is of order $p$. If $p X|x|$, then $|G /\langle x\rangle|$ is divisible by $p$. By induction, $\exists a \in G$ s.t. $a<x>$ is of order $p$. then $p||a|$. since $(a<x>)^{|a|}=1$. then $a^{\frac{|a|}{p}}$ is of order $p$. Campus

Then $\exists x \in z(6)$ is of order $p$. so $p^{e-1} \|(G /\langle x\rangle)$. By induction, $G /\langle x\rangle$ has a subgrap of the form $H /\langle x$ of order $p^{e-1}$. Then $H$ is a subgraup of order $p^{e}$. $\square$

Thm (second Sylow thm)
Sylow $p$-subgraps are conjugate.
pf: Let $\left.\Omega=s_{p_{1}}, p_{2}, \cdots, p_{n}\right\}$ be the set of Sylow $p$-subgp: Let $P=P_{1}$ and let $\left.\Delta=S P x \mid x \in G\right\} .|\Delta|=[G . P]$ Then $G$ acts on $\Delta$ by right multiplication: $g: P_{x} \mapsto P \times g$.
Then $|\Delta|=\frac{|G|}{|P|}$, coprime to $p$ and the action of $G$ on $\Delta$ is transitive.
Let $H<6$ s.t. $|H|=p^{d} \leqslant p^{e}$. Then $H$ ading on $\Delta$ is intransitive. (otherwise $m / p^{d}$, contradiction). Suppose $H$ acting on $\Delta$ partitions $\Delta$ into orbits $\Delta_{1}, \cdots, \Delta r$. Then $\left|\Delta_{i}\right|||H|=$ and $|\Delta|=\left|\Delta_{1}\right|+\cdots+|\Delta r|$. So $\left|\Delta_{i}\right|=1$ for some isince: $p \nmid k$ i.e. $\quad P \times h=P \times$ for some $x$, for each $h \in H$.

$$
\begin{aligned}
x H x^{-1} & <P \\
H & <x^{-1} P x
\end{aligned}
$$

If $|H|=p^{e}, H=x^{-1} p x$

## Thm (Third Sylow thm)

Let $|G|=p^{e} m$ with $\operatorname{gcd}(p, m)=1$ and let $n_{p}$ denotes the number of sylow $p$-subgraps of 6 . Then $n p / m$ and $n p \equiv 1(\bmod p)$.

## Group actions

Let $G$ be finite. $\quad l \neq N \underset{\min }{\rightarrow}$. Then $\bar{G}=G / N$ is a group with $|\bar{G}|<G$. So ${ }^{\text {min }} G=N \cdot \bar{G}$. (extension).

## Lemma

If $N$ is a minimal normal subgrap of $G$, then $N=S_{1} \times \cdots \times$
$S r$ where $S_{1} \simeq S_{2} \simeq \cdots \simeq S_{r}$ simple.
i.e. $N=S^{r} \simeq S_{1} \times \cdots \times S_{r}$. Further $N=C_{p}^{r}$ or $S$ is nondeling $p f$ :
If $N$ is simple, then $O K$.
Assume that $N$ is not simple. Let $M$ be a minimal normal subg.p of $N$. by induction, $M=\pi x \cdots x T_{m}$ where $T_{1} \simeq T_{2} \simeq$ Tm simple.
For $g \in$ 6, the conj $M^{9}=g^{-1} M g$ is a minimal normal subgrap of $N$, either $M^{g}=M$ or $M \cap M^{g}=\{1\}$ by minimality.
The normal closure of $M$ under $G$ is

$$
M^{6}=\left\langle M^{8} \mid g \in G\right\rangle=M \times M^{g_{1}} \times \cdots \times M^{g_{l}}
$$

(Note that if $M \cap M^{g}=1$, then $\left\langle M, M^{g}\right\rangle=M \times M^{g} \sin a_{g} M \nabla N, M^{g} \triangle$,
Also, since $M=T_{1} \times \cdots \times T_{m} \Rightarrow M^{g}=T_{1}^{g} \times \cdots \times T_{m}$

Hence $M^{9}=T^{m \lambda}$.
$M^{6} \& G$ and $M^{6}<N \Rightarrow N=M^{6}=T^{m l}$

Now change notation: $\quad M \underset{\min }{\Delta} G . \quad M=\left\{\begin{array}{l}G_{p}^{r} \\ T \times \cdots \times T r \text { nonabelian }\end{array}\right.$

$$
G / C_{G}(M)=N_{G}(M) / C_{G}(M) \leqslant \text { Aut }(M) \text { where } M \& G .
$$

Q: For Ti nonabelian, want to find $\operatorname{Aut}\left(\pi \times \cdots \times T_{r}\right)$ For $g \in \operatorname{Aut}(M)$, we have $T_{i}^{g} \Delta M$ as $T_{i} \Delta M$.
Claim: $\pi^{g}=\pi_{j}$ for some $j$.
pf:

$$
T_{i}^{g} \& T_{1} \times \cdots \times T_{r}=M
$$

Suppose $T^{g} \neq T_{2} \times \cdots \times \operatorname{Tr}^{\Delta} L$, then $T_{i}^{g} \cap L=1$ since $T i^{g} \cap L \triangle T i^{g}$
Then since $T_{i}^{g} \subset M=\pi x \cdots x T_{r}, \exists\left(t_{1}, y\right) \in T^{g}$ where $t_{1} \neq 1$ and $y \neq 1, y \in L$

$$
(t, y)^{(t, 1)}=(t, t, y) \quad \forall t \in T_{1} .
$$

Since $T_{i}$ is nonabelian and simple, 㸲 $Z\left(T_{i}\right) \Delta T_{i}$, hence

$$
\exists t \in \Pi \text { s.t. } t_{1}^{t} \neq t_{1} \text { and }\left(t_{1}, y\right)^{-1}\left(t_{1}^{t}, y\right)=(\underbrace{t_{1}^{-1} t_{1}, t}_{t_{0}}, 1) \in T
$$

$$
\begin{equation*}
\left\langle\left(t_{0}, 1\right)^{(t, 1)} \mid(t, 1) \in \pi \times L\right\rangle \Delta \pi_{i}^{g} \tag{11}
\end{equation*}
$$

$$
\left\langle\left(t_{0}^{t}, 1\right) \mid t \in \Pi\right\rangle \forall T_{1} \Rightarrow\langle \rangle=T_{1} \Rightarrow \pi_{1}^{g}=T_{1} .
$$

On the other hand, $T_{i}{ }^{9}<T_{2} \times \cdots \times T r$, continue.
⇒ Aut $L M$ ) induces a permutation graup on $S T_{1}, T_{2}, \cdots, T_{r}$ ? with kernel Aut $(T 1) \times \cdots \times$ Aut $(T r)=: K$.
s.t. $\operatorname{Aut}(M) / K=S_{r} \Rightarrow \operatorname{Aut}(M)=K \cdot S_{r}=\operatorname{Aut}(T)^{r} \cdot S$

## Example

structure of 6 where $|6|=72$
Suppose it is solvable, $\exists N \mathrm{~m}_{\min } G$.

$$
N \simeq T^{r} \quad \pi T^{r} \quad 172 \Rightarrow \min _{N \leqslant C_{3}^{2}} \text { or } C_{2}^{3}
$$

by conj. action
T △ AutCT
Autt (T)/T $\cong$ att relatively. small

Date

Campus

Week 4
(Burnside)
If $G$ is a finite group of which sylow subgps are all cyclic, then $G$ is solvable.

Def transfer from $G$ to $H / H^{\prime} \quad(H<6)$
$\dot{G}$ finite. $H<G \quad \Omega=[6: H]=\{H \times \mid \times \dot{\epsilon} 6\}=\left\{H \mid x_{1}, \ldots, H x_{n}\right.$

$$
\hat{g}: H x_{i} \rightarrow H x_{j}=H x_{i} g
$$

So $x_{i} g \in H x_{j}$ and $x_{i} g=h x_{j}$
$\hat{g}$ induces a permutation on $\{1,2, \cdots, n\}$ denoted by $z(g)$.
i.e. $i^{2(g)}=j$

$$
h_{i}(g)=x_{i} g x_{j}^{-1}=x_{i} g x_{i}^{-1} \nu(g) \in H .
$$

Let

$$
\begin{aligned}
V_{6 \rightarrow H}: G & \rightarrow H / H^{\prime} \\
g & \mapsto \prod_{i=1}^{n} X_{i} g X_{i}^{-1}(g) \cdot H^{\prime}=\prod_{i=1}^{n} h_{i}(g) H^{\prime}
\end{aligned}
$$

called a transfer from 0 to ${ }^{1=1} \mathrm{H} / \mathrm{H}^{\prime}$

## lemma

11) $V_{6} \rightarrow H$ is a homomorphism from $G$ to $H / H^{\prime}$
(2) $V_{0} \rightarrow H$ does not depend on the choices of $x_{1}, \cdots, x_{n}$.
(3) writing $\hat{g}$ in cycle form.

$$
V_{G \rightarrow H}(g)=\prod_{i=1}^{t}\left(x_{i} g^{l_{i}} x_{i}^{-1}\right) H^{\prime}
$$

pf:
(1) For $g_{1}, g_{2} \in G$

$$
\begin{aligned}
& V_{G \rightarrow H}\left(g_{1} g_{2}\right)=\prod_{i=1}^{n} h_{i}\left(g_{1} g_{2}\right) H^{\prime} \\
& =\prod_{1}^{1=1} \chi_{1}^{1} g_{1} g_{2} \chi_{1}^{-1} z\left(g_{1} g_{2}\right) H^{\prime} \in H / H^{\prime} \text {, abelic } \\
& =\prod x_{i} g x_{i} z_{\left(y_{1}\right)}^{-1} H^{\prime} \pi x_{i} z\left(y_{1}\right) g_{2} x_{1}^{-1} z\left(y_{1}\right) z\left(y_{2}\right) H \\
& =V_{6 \rightarrow H}\left(a_{1}\right) V_{G \rightarrow H}
\end{aligned}
$$

(2) Let $y_{1}, \cdots, y_{n} \in G$ s.t. $H y_{1}=H x_{i}, 1 \leqslant 1 \leqslant n$. Then $y_{i}=t_{i} x_{i}$, for some tieH, $1 \leqslant 1 \leqslant n$.
For $g \in 6$, let $\left.y_{i} g=k_{i}(g) y_{i} u g\right) \quad$ Then $k_{i}(g)=y_{i} g y_{i}^{-1}(g) \in H$. The corresponding transfer.

$$
\begin{aligned}
\tilde{v}_{6 \rightarrow H}(g)=\prod_{i=1}^{n} k_{i}(g) H^{\prime} & =\prod y_{i} g y_{i}^{-1} z(g) H^{\prime} \\
& =\prod t_{i} x_{i} g\left(t_{i} z(g) x_{i} z(g)\right)^{-1} H^{\prime} \\
& =\pi t_{i}\left(x_{i} g x_{i}^{-1} z(g)\right) t_{i}^{-1} z(g)
\end{aligned}
$$

$\mathrm{H} / \mathrm{H}^{\prime}$ abelian
$\stackrel{H \text { arelian }}{=} \pi x_{i} g x_{i}^{-1}(g) H^{\prime} \pi t_{i} H^{\prime} \pi t_{i}^{-1}(g)$
$=\pi x_{i} g x_{i}^{-1}(g) H^{\prime}$
$=V_{6} \rightarrow H(g)$.
(3) $\hat{g}$ is a permutation on $\Omega=\left\{H x_{1}, \cdots, H x_{n}\right\}$.
<g> parititichs $\Omega$ into $t$ orbits: $\left(H_{x_{i}} g^{l_{i}}=H x_{i}\right) (H x_{1}, H x_{1} g, \cdots, \underbrace{}_{l_{1}-1} H x_{1} g^{l_{1}-1}),\left(H x_{2}, H x_{2} g, \cdots, H x_{2} g^{l_{2}-1}\right), \cdots,\left(H x_{t}, \cdots, H x_{x} \bigcirc_{i}^{l}\right.$ Take $x_{1}, x_{1} g, \cdots, x_{1} g^{l_{1}-1}, x_{2}, x_{2} g, \cdots, x_{2} g^{l_{2}-1}, \cdots, x_{t}, x_{t} g_{1}, x_{t} g^{l_{t}-1}$ as reps. The corresponding transfer is

$$
\begin{align*}
V_{G \rightarrow H} \quad(g) & =\prod x_{i} g\left(x_{i} g\right)^{-1}\left(x_{i} g\right) g\left(x_{i} g\right)^{-1} \cdots x_{i} g^{l_{i}^{-1}} g x_{i}^{-1} H  \tag{0}\\
& =\prod\left(x_{i} g^{l_{i}} x_{i}^{-1}\right) H^{\prime} \tag{0}
\end{align*}
$$

Def $p$-nilpotent
A grap $G$ is called $P$-nilpotent if $G=N P$ where $N \varangle G$ and $P$ is a sylow $P$-subgranp and $N \cap P=1$ ( $\Leftrightarrow$ If $G$ has a normal subgp $N$ s.t. $|G| N\left|=|G|_{p}=|P|\right.$ where $p$ is a prime divisor of $(G)$ and $P$ is any sylow p-subgrap of 6 ).

## Thm (Burnside)

Let $G$ be a finite group and $P$ a Sylow $p$-subyp. If $N_{G}(P)=C_{G}(P)$, then $G$ is p-nilpotent.
of: (use transfer to construct $\operatorname{ker} V_{G \rightarrow p}=N$.).
Since $N_{G}(P)=C_{G}(P), P$ is abelian $\left(P \leq N_{G}(P)\right.$ hence $\left.P \subseteq C_{6}(P)\right)$, so $P^{\prime}=1$.
Let $g \in P \backslash S 1\}$. Then

$$
V_{6} \rightarrow p(g)=\prod_{i=1}^{t} x_{i} g^{l_{i}} x_{i}^{-1}
$$

As $g^{l_{i}}$, $x_{i} g^{l_{i}} x_{i}^{-1} \in P$ and ${ }_{1=1}^{1} P$ is abelian, $C_{6}\left(g^{l_{i}}\right) \geqslant P$. $C_{G}\left(x_{i} g^{l i} x_{i}^{-1}\right)=x_{i} C_{G}\left(g^{l i}\right) x_{i}^{-1} \geqslant p$, Hence $\operatorname{Co}\left(g^{l i}\right) \geqslant p$ and $C_{6}\left(g^{l_{i}}\right) \geqslant x_{i}^{-1} P x_{i}$.
By Sylow thm, there exists $u \in C_{0}\left(g^{l i}\right)$ s.t.

$$
\begin{aligned}
u^{-1}\left(x_{i}^{-1} p x_{i}\right) u & =p \\
\| & =p \\
\left(x_{i} u\right)^{-1} p\left(x_{i} u\right) & =p
\end{aligned}
$$

Thus $x_{1} u \in N_{G}(P)=C_{G}(P) \Rightarrow x_{i} \in C_{G}(P) \leqslant C_{G}\left(q^{l_{i}}\right)$
Therefore $\quad V_{G} \rightarrow P(g)=\prod_{i=1}^{t} g^{l_{i}}=g^{l_{1}+\cdots+l_{t}}=g^{|G \cdot P|} \neq 1$
(since $g \in P \backslash S B$ )
which yields $V_{6} \rightarrow P(P)=P$ (since it is injective by above discus and $|G|<\infty) \Rightarrow V_{G} \rightarrow P(G)=P$.
let $\mathrm{Ker} V_{G \rightarrow P} \triangleq$. Then by iso thm $G / k \simeq P$ so $k \triangle G$

## Thm (Burnside)

Let 6 be a finite graup of which sylow subgrups are all cyclic. Let $p$ be the smallest prime divisor of $G$ and let $P$ be a Syluw $P$-subgranp if $G$.
Then (1) $G$ is p-nilpotent
(2) $G$ is solvable.

Pf: Now P is cyclic and No (P) / Co (P) $\lesssim A u+(P)$ where $\mid$ Aut $(p) \mid=p^{e-1}(p-1)$ whose prime divisor $\leq p$. Then we can only have $N_{6}(P) / C_{6}(P)=1$ since $P^{e} \| K_{6}(P) \mid$ as $p \leq C_{G}(p)$, all prime divisors of $L H S>p$. Then $N_{0}(P)=C_{0}(P)$ and so $G$ has a normal subgrup $K$ with $|k|=|G| /|P|$.

Let $\alpha_{1}$ be the smallest prime divisor of $|K|$. Then $K$ is $O p_{1}$ - nilpotent and has a normal subgp $k_{1}$ with $\left|k_{1}\right|=|K| / \mathbb{P}_{1} \mid$. Repeating this process, we have that

Gok , 兄 $K_{1} D_{x} \cdots D_{x} Q$
Sylow $q$-subgp. where $q$ is the largest prime divisor of 161 .

## Remark $\quad Q \triangleleft G$.

Pf: Suppose $Q \Delta K_{n} \Delta K_{n-1}$.
Then $\forall g \in K_{n-1}, \quad g^{-1} Q g \subseteq g^{-1} K_{n} g=K_{n}$. Since $Q \triangleleft K_{n}$, $K_{n}$ has only one sylow $q$-subgraup, then $g^{-1} \theta g=\theta$, hence $Q \triangleleft K_{n-1}$. Repeat this process, $\theta \Delta G$.

## Corollary

( ⇒ any Sylow 2-subgrap are cyclic).
If a Sylow 2-subgrap of 6 is cyclic, then 6 is 2-nilpoteor hence solvable.
Pf: need to use Feit-Thompson- $|G|$ odd $\Rightarrow G$ solvable Then $N \& G / N$ both solvable $\Rightarrow G$ solvable.
prop
Let $G$ be a finite nonabelian simple graup. Let $p$ be a prime divisor of 101 .
(1) If $p$ is the smallest prime divisor, then $G$ is divisible be, $p^{3}$ or 12 .
(2) If $|G|=p m$ with $\operatorname{gcd}(p, m)=1$. Then for a Sylow $p$-subgp $p$,
(i) $\mathrm{C}_{6}(\mathrm{P}) \neq \mathrm{N}_{6}(\mathrm{P}) \neq 6$
(ii) $\left|N G(P) / C_{G}(P)\right| \mid(P-1)$.

Pf:
(1) Suppose $|G|$ is not divisible by $p^{3}$. Then $p^{2}| ||G|$ otherwise a Sylow $p$-subyp $P=C_{p}$ so that $N_{G}(P)=C_{G}(P)$. and $G$ is $p$-nilpotent, contradiction. since nontrivial
Then $|P|=p^{2}$ and so $p \simeq C_{p^{2}}$ or $C_{p} \times C_{p}$... The former would imply $G$ is $p$-nilpotent, not possible. Hence $P \simeq C_{p} \times C_{p}$ and $N \sigma(P) \neq C_{G}(P)$. So $N \sigma(P) / G_{G}(P) \simeq A$ at $(P)=A$ ut $(C p \times(p) \simeq G L(2, p)$. (multiply by scalars = addition).
Now $|G L(2, p)|=p\left(p^{2}-1\right)(p-1)=p(p+1)(p-1)^{2}$.
Let $r$ be a prime divisor of $|N \sigma(p) / C G(p)|$. Then $r \mid p(p+1)$.
And $r>p\left(P \simeq C_{p} \times C_{p}\right.$ so $\left.P \in C_{6}(P)\right)$ so $p=2$ and $p+1=3$.
Hence $12|16|$.
(2) easy.

Example If $|G|=5.7 .13$, then $G$ is aydic
Let Gpdenotes a sylow $p$-subgraup.
$\begin{array}{ll}-G_{13}<G & \quad\left(N_{6}\left(G_{13}\right) / C_{6}\left(G_{13}\right) \leq A_{4}+\left(G_{13}\right)=C_{12}\right. \\ -G \text { is } 5 \text {-nilpotent. } & \rightarrow N_{6}\left(G_{13}\right) \leq 0\end{array}$

$$
\rightarrow N_{6}\left(G_{13}\right) \leq\left[4 G_{0}-\left(G_{13}\right)\right.
$$

$\rightarrow G$ is 13 -nilpotent. $G=G_{12} \times G_{15}, 7$

## 22

## Week 5

## solvable

Let $\sigma^{\prime}$ be the commutator subgrap of 6 . let $G^{\prime \prime}=G^{(2)}=\left(G^{\prime}\right)^{\prime}$ and $G^{(n)}=\left(G^{(n-1)}\right)^{\prime} G \triangleright G^{\prime} \nabla G^{\prime \prime} \nabla \cdots \nabla G^{(n)} \nabla \cdots$
example $G^{\prime}$ of $G$
(1) 6 simple \& $G$ not ablian $\left(G / 6^{\prime}\right.$ abelian \& $\left.6^{\prime} \neq 1\right) \Rightarrow G^{\prime}=6$
(2) $G=\operatorname{Sn} . \quad\left\{\begin{array}{l}G^{\prime}=A_{n} \text { if } n \geqslant 5 \\ G^{\prime}=A_{4} \text { if } n=4\end{array}\right.$ (An generated by all. 3 -cycles

$$
\left.(123)=(12)(13)(12)^{-1}(13)^{-1}\right) O
$$

If $G$ is finite,

$$
G \nabla_{x} G_{(n)}^{\prime} \nabla_{x} G^{(2)} \square \cdots \nabla_{(n+1)} \nabla^{(n)} \triangleright G^{(n+1)}
$$

either $\sigma^{(n)}=\sigma^{(n+1)}=\sigma^{(n+2)}=\cdots=\sigma^{(\infty)}$ or $\sigma^{(n)}=1$
notation.
Def solvable \& perfect
(1) $\sigma$ is solvable if $\sigma^{(n+1)}=1$ for some $n$.
(2) $G$ is called a perfect grapp if $G=G^{\prime}$

## O

O

## Example perfect graups

(1) all simple graps (nonabelian)
(2) An with $n \geqslant 5 . \quad(123)(345)(123)^{-1}(345)^{-1}=(143)$
(3) $S L d(q)$ when nonsolvable i.e. $d \geqslant 2,(d, q) \neq(2,2),(2,3)$

$$
\downarrow Z(s L d(q))=Z(d, q-1) .
$$

prop properties of being solvable

1) $O$ is solvable, then so is each subgroup and each factor grapp of G
（2）If $N \triangleleft G$ s．t．$N$ and $G / N$ are solvable，then $G$ is soluabl （ 6 is in $N$ or $G / N$ ）
（3）$p$－groups are solvable．（use（2）and induction and $Z(6)$ ）$G$ is Nontrivial）

Lemma If $P$ is a nonsylow $p$－grap of 6 ，then $P \nsubseteq N_{6}(P)$
Def double coset（双陪集）
$P<G$ ．
$\left.P \times P=S h_{1} \times h_{2} \mid h_{1}, h_{2} \in P\right\}$ ．a double coset of $P$ in $G$
Lemma \＃of right cosets of $P$ contained in $P x P$ is equal to $|P| /\left|P^{x} \cap P\right|$
pf：$P \times P=\bigcup_{h \in P} P \times h$
$P \times h_{1}=P \times h_{2} h_{A} \in P, h_{1}, h_{2} \in P \Rightarrow h_{1} h_{2}^{-1} \in P^{x} \Rightarrow h_{1} h_{2}^{-1} \in P^{x} \cap P_{1}$
of of lemma：
Decompose $G=\frac{\bigcup_{x \in G}}{X \in G}$（Note that for $x, y \in G, P_{x} P=P y P$ or $P x P \cap P y P=\phi)$ ．
－$P I P=P$ has a unique coset of $P$ ．
－$|G|=\left|P_{1} P\right|+\left|P x_{1} P\right|+\cdots+\left|P x_{t} P\right|$
consider \＃of cosets of $P$ on both sides．
LHS：divisible by $P$
RHS：$\left|P_{x i} P\right|=|P| /\left|P^{x} \cap P\right|$ is either divisible by $P$ or is 1 ． Then for some $x_{j},\left|p_{x_{j}} \cap p\right|=P \quad x_{j} \notin P$ ，so $p^{x_{j}} \cap P=P$ and $P_{j}=P$ i．e．$x_{j} \in N_{G}(P)$ ，so $P \in N_{G}(P)$ ．

Thm subgps \& normal subgps of $p$-grapps. Let 6 be a $p$-grap
(1) If $H<6$, then $H<N_{6}(H)$.
(2) If $H \widetilde{\max }_{6} 6$, then $H \triangleleft G$ and $|G| /|H|=P$ (consider $\left.G / H\right)$
(3) $G$ is solvable (find $H \underset{\text { max }}{6}$ 6, use induction).
(4) $Z(6)>1$
(5). If $N$ is a normal subgrap of $G$ and $|N|=P$, then
◯ $N \leqslant Z(O)$.
pf of (5):

By NC lemma,

$$
G / C_{G}(N)=N_{G}(N) / C_{G}(N) \leqslant \operatorname{Aut}(N)=\mathbb{Z}_{P-1} .
$$

◯

Then $G=\operatorname{Co(N)}$ so $N \leqslant Z(G)$.
corollary A group of order $p^{2}$ with $p$ prime is abelian.
◯
of: $z(6)$ is nontrivial.

Q: what about $|6|=p^{3}$ ? (Ds, Q8 not abelian).

Thm structure of 6 when $|G|=p^{3}$ ◯
$|6|=p^{3}$. Then either $G$ is abelian ( 3 types) or
(1) for $p=2, G=D_{8}$ or $Q_{8}$ ◯
(2) for $p \geqslant 3, \quad G=Z p^{2}: Z p \quad$ or $\quad G=\langle a, b, c| a^{p}=b^{p}=c^{p}=1$,
$[a, b]=c>$
("exponent" $=p$ )
$\mathrm{p}^{+2}$
$\mathrm{P}_{+}^{1+2}$
$\left.{ }^{\downarrow} \max .|g| g \mid g \in 6\right\}$.
of: Assume $G$ is nonablian.
(1) suppose 6 has an element a of order $p^{2}$

Brapabove thm, $\langle a\rangle \varangle \sigma$. Then there is $b \in G \mid\langle a\rangle$ and
$b^{p} \in\langle a\rangle . \quad(b<a\rangle$ is of order $p$ in $\left.G /\langle a\rangle\right)$.
If $b^{p}=1$, then $G=\langle a\rangle$ : $\langle b\rangle$ as in 11) for $p=2$, (2) for $p>$, Suppose $|b|=p^{2}$. Then $b^{p}=a^{l p}, a^{p}=a^{\lambda}$ for some integer. If $p=2$, then $a^{b}=a^{\lambda}=a^{-1}$ and $b^{a}=b^{8}=b^{-1}\left(a^{b}=\phi, d^{2} \propto 0\right. 6=\left\langle a, b \cdot \mid a^{4}=b^{4}, a^{b}=a^{-1}, b^{a}=b^{-1}\right\rangle \rightarrow$ unique elt forder: $a^{2}=b^{2}$.
Suppose $p$ is 0 dd. Then $a^{b}=a^{1+k p}$ for some $k$ with $1 \leq k \leq p-1\left(a^{b^{p}}=a=a^{\lambda^{p}} \Rightarrow \lambda^{p} \equiv 1\left(\bmod p^{2}\right) \Rightarrow \lambda^{p} \equiv 1(\bmod p)\right.$
$\left.\Rightarrow \lambda^{p} \equiv \lambda \equiv 1(\bmod p) \Rightarrow \lambda=1+k p\right)$.
Claim: $\left(b a^{-l}\right)^{p}=1$.
$b a^{-l} \cdot b a^{-l}=b^{2} b^{-l} a^{-l} b a^{-l}=b^{2} a^{-2 l-l k p}$.
$\left(b a^{-l}\right)^{p}=b^{p} a^{-p l-l k p^{2}}=1$.
Let $b^{\prime}=b a^{-l}$ as in (2).
(2) Suppose 6 does not have elts of order $p^{2}$.

Then each non identity elt of $G$ is of order $p \Leftrightarrow p$ must be odd since if H elt of 6 is of order 2 then 6 is abdia Consider $G^{\prime}$.
Since $Z(6)$ is nontrivial, pick $Z \in Z(6)$, then $Z_{p}=\langle Z\rangle \Delta( |G /\langle z\rangle|=p^{2} \Rightarrow G /\langle z\rangle$ is abelian by above discussion.
Hence $G^{\prime}\left\langle\langle z\rangle \Rightarrow G^{\prime}=Z_{p}=\langle[a, b]\rangle\right.$
let $c=[a, b]$. Then $a^{p}=b^{p}=c^{p}=1, c=[a, b]$ ash (2). $\left(\langle a, b\rangle\langle G|,\langle a, b\rangle \mid \neq p\right.$ and $p^{2}$ since $a, b$ does not commut So $\langle a, b\rangle=6 . \quad$ ).
＊consider Aut（6）of $|6|=p^{3}$
$A_{\text {Ant }}\left(D_{8}\right) \simeq D_{8}$

Aut $\left(Q_{8}\right) \simeq S_{4}$

Aut $\left(Q_{8}\right) \simeq S_{4}$

Aut $\left(\mathbb{Z}_{p^{2}}: \mathbb{Z}_{p}\right) \simeq P: Z_{p-1}$
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-26.jpg?height=100&width=243&top_left_y=632&top_left_x=591)

Aut $\left(p_{+}{ }^{1+2}\right) \simeq G L(2, p)$ ．
e．g．$G=P_{-}^{1+2}$ or $P_{+}^{1+2}$
$G^{\prime}=Z(0)=Z_{p}$ ．

Let $z \in \operatorname{Aut}(6)$ ．Then $\left(G^{\prime}\right)^{2}=G^{\prime}\left((z(6))^{2}=z(6)\right)$ ，and
$\tau$ induces an automorphism $\bar{\tau}$ of $G / G^{\prime} \simeq \mathbb{Z}_{p} \times Z_{p}$ ：
（1）$G=\langle a\rangle:\langle b\rangle=Z_{p^{2}}: Z_{p}$ then $G^{\prime}=\left\langle a^{p}\right\rangle$
This is because $a^{b}=a^{i} \Rightarrow a^{b^{p}}=a^{i p}=a^{1} \Rightarrow i=1+l p \Rightarrow\left(a^{p}\right)^{b}=a^{p O}$
Then $G / G^{\prime}=\langle\bar{a}, \bar{b}\rangle=Z_{p} \times Z_{p}$（abelian）s．t． $\bar{a}^{2} \in\langle\bar{a}\rangle$
$\Leftrightarrow(|z| ; p)=1$ then $|\bar{z}| \mid p-1)$
（2）$G=P_{+}^{H+2}$ then $\bar{z} \in \operatorname{Aut}(\langle\bar{a}\rangle x\langle\bar{b}\rangle)$
$z$ fixes $\sigma^{\prime}=\langle[x, y] \mid x, y \in \sigma\rangle$－双线型
$z \in S p(2, p) \simeq S L(2, p)$ extension scular multiplication

Week 6. Free graps and minimal normal subgrap.

## Free graps

Def free grap.
Let $\left\{x_{1}, \cdots, x_{n}\right\}$ be a set and $\left\{x_{1}^{\prime}, \cdots, x_{n}^{\prime}\right\}$ s.t. $\left\{x_{1}, \cdots, x_{n}\right\}$ ( $\left\{x_{1}^{\prime}, \ldots, x_{n}^{\prime}\right\}=\phi$. Let $S=\left\{x_{1}, \ldots, x_{n}, x_{1}^{\prime}, \cdots, x_{n}^{\prime}\right\}$.
Let $X$ be the set of all words of finite sequence compounde of the elements of $S$. For $u, v \in X$,

$$
\begin{aligned}
& u=x_{i}, \cdots x_{i r}, \\
& v=y_{j 1} \cdots y_{j t},
\end{aligned}
$$

define (1) $u \cdot v=x_{i 1} \cdot x_{i 1} y_{j 1} \ldots y_{j t}$
another way of definition. "reduced words": $x_{i}$ and $x_{i}{ }^{i}$ are no godjacent gdgyacent $^{\prime}$
$V_{1}=U_{1} v$
(2) If $u=u_{1} x_{i}$ and $v=x_{i}^{\prime} v_{1}$, then $u v=u_{1} x_{i} x_{i}^{\prime}$. This identification is an equivalent relation. which gives rise to an equivalent partition of $x$. Let $[u v]$ denote the class containing $u v$. Then $[u]$ denotes the class containing $u$ (say $\left.n, u x_{1} x_{1}^{\prime}, u x_{i} x_{i}^{\prime}, \ldots\right)$ and $[u][v]=[u v]$.
Then $\bar{x}=\{[\bar{u}] \mid u \in X\}$ forms a grap, called a free grapp generated by $x, \cdots, x_{n}$... all the reduced hords i.e. $\bar{x}=\left\langle x_{1}, \cdots, x_{n}\right\rangle$, denoted by form a grapp. product is also def called the rank of $F_{n}$. a reduces nord.

Example $\quad X=\left\langle X_{1}, X_{2}\right\rangle$
If $g \in X$. then $g=x_{1}^{m_{1}} x_{2}^{m_{2}} x_{1}^{m_{3}} \cdots x_{2}^{m_{t}} \cdots$ or $x_{2}^{m_{1}} x_{1}^{m_{2}} \cdots x_{1}^{m_{t}} \cdots$ nhere $\left|m_{i}\right| \geqslant$
(1) $x=\left\langle x_{1}, x_{2} \mid x_{1}^{2}=1, x_{2}^{2}=1\right\rangle$ - infinite dihedral grap
(2) $\left.x=\left\langle x_{1}, x_{2}\right| x_{1}^{2}=x_{2}^{2}=1,\left|x_{1} x_{2}\right|=m\right\rangle-D_{2 m}$
(3) $x=\langle x, y||x|=m,|y|=n,|x y|=l\rangle$ - triangle grou
◯
＊triangle grap $T_{m}, n, l$ consider $\left(\frac{1}{m}+\frac{1}{n}+\frac{1}{l}\right) \geqslant$

经线 ◯
◯

对应球面上几向洁构的对称。

O
◯
$\_\_\_\_$
Case $1 \frac{1}{m}+\frac{1}{n}+\frac{1}{l}>1$

正团面体 立方体对称
$\_\_\_\_$
$m=2$

立方体
（与正面
体对偶）
$n=3 \Rightarrow l=3$ or 4 or 5 ．

## Further analysis shows $G=A \psi, S \psi$ or $A$ s

对喁：每个面的中心连起来）![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-28.jpg?height=69&width=69&top_left_y=1347&top_left_x=1700)
$>\pi \quad$ spherical
$=\pi$ Enclidean
$<\pi \quad$ hyperbolic

WLOG．assume $m \leq n \leq l$ ．then if $G \neq \sin , l>1$

$$
\begin{align*}
& n+n+l>1 \\
& G=\langle y\rangle \text { cyclic } \Rightarrow G=\mathbb{Z} n . \\
& n=2 \Rightarrow G=D_{2 l} \tag{0}
\end{align*}
$$

◯

$$
\text { 正 }+ \text { 面 }
$$

Case 2

$$
\frac{1}{m}+\frac{1}{n}+\frac{1}{l}=1
$$

$(m, n, l)=(3,3,3),(2,3,6)$ or $(2,4,4)$
$\_\_\_\_$
$G=\left\langle x, y \mid x^{m}=y^{n}=(x y)^{l}=1\right\rangle$ infinity，solvable． $\_\_\_\_$

![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-28.jpg?height=311&width=399&top_left_y=1673&top_left_x=224)
$\stackrel{\rightharpoonup}{\text { fact }}(2,3,6)$

![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-28.jpg?height=195&width=287&top_left_y=1646&top_left_x=659)
$\stackrel{\rightharpoonup}{\text { fact }}(2,3,6)$

in fact 正六边形

止三角形 sume group（正三角形的对偶）
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-28.jpg?height=171&width=236&top_left_y=1969&top_left_x=333)

![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-28.jpg?height=205&width=352&top_left_y=1670&top_left_x=1122)
正方形（ $2,4,4$ ）

![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-28.jpg?height=180&width=409&top_left_y=2016&top_left_x=753)
（只有这三种正多边形能把平面铺满）。
Case $3 \quad \frac{1}{m}+\frac{1}{n}+\frac{1}{l}<1$ $\_\_\_\_$
Campus
$G=\left\langle x, y \mid x^{m}=y^{n}=(x, y)^{l}\right\rangle$ infinity，nonsolvable

## Example

$$
\begin{aligned}
& S_{n}=\langle(12),(12 \cdots n)\rangle \\
& \Rightarrow S_{n}=\left\langle x, y \mid x^{2}=y^{n}=1,(x y)^{n-1}=1, \cdots\right\rangle \\
& \text { must be more. } \\
& \text { othernise by above discussion } \\
& \text { 1> } \frac{1}{2}+\frac{1}{n}+\frac{1}{n-1} \text { nlange } \\
& \text { ⇒ infinite. contradiction. }
\end{aligned}
$$

Thm Every $f \cdot g$ grapp is a homomaphic inage of a free grow Let $G=\left\langle g_{1}, \cdots, g_{n}\right\rangle$, Then $F_{n}$ is homomopplic to $G$.
Pf: $\varphi: F_{n} \rightarrow G$

$$
\begin{aligned}
& {\left[x_{i}\right]} \\
& {\left[x_{i}\right]^{-1}} \\
& {[\phi]} \\
& {[\phi]}
\end{aligned} g_{i}{ }^{-1} 1 ~ 1 ~ 0.10
$$

let $N=\operatorname{ker} \varphi=\{x \mid \varphi(x)=1\}$
Then $F_{n} / N \simeq G$.
We then call $\left\{g_{1}, \ldots, g_{n}\right\}$ generating elements (or generators) \& $S \varphi(x)=1 \mid x \in N\}$ generating relations
e.g. $D_{2 m}=\left\langle x, y \mid x^{2}=y^{2}=(x y)^{m}=1\right\rangle$

$$
=\left\langle a, y \mid a^{m}=y^{2}=1, a^{y}=a^{-1}\right\rangle .
$$

## Minimal normal Subgraps

transitive in ↑ each
$\Omega=\{1,2, \ldots, n\}$
If $H<\operatorname{Sym}(\Omega) \simeq S_{n}$ is intransitive, then $H \leq S_{K} \times S_{n}-$
let $6 \leqslant \operatorname{Sym}(\Omega)=\operatorname{Sn}$.
Def quasi-primitive grap.
A grap 6 is called a quasi-pnimitive grapp if all its nontrivial normal subgraups are transitive.

Counter example

$$
\begin{equation*}
G=D_{2 n}<S_{n} \tag{0}
\end{equation*}
$$

$$
=\left\langle( 1 n ) \left( 2 \begin{array}{c}
n-1) \cdots, \\
y
\end{array}, \begin{array}{c}
(12 \cdots n) \\
x
\end{array}\right.\right.
$$

when $n$ is even, $\left\{b, a^{2} b, \cdots, a^{n-2} b\right\} \triangle G$, but it has $\frac{n}{2}$ elements so it can not be transitive.
prop let $G$ be a quasi-primitive grap. Let $M$ be a minimal normal subyraup of 6 . then
(1) $M$ is Transitive (if 6 any group, this is not always true. e.g. $G=\{(12)(34),(13)(24),(14)(23), 1\}<\{4\} A=\{1,(12)(34)<6$ not ransitive $($
(2) $M=T^{l} \simeq T_{1} \times \cdots \times T_{l}$ where $l \geqslant 1, T_{1} \simeq \cdots \simeq T_{l} \simeq T$ are simple.

Lemma If $M \triangle G$ is transitive, then $G=M G w$ for any. $O$, of: Given $g \in G$, let $w^{\prime}=w^{g}$, Sina $M$ is transitive on 1,0 $\exists m \in M$ s.t. $w^{m}=w^{g}=w^{\prime}$ Thus $m^{-1} g=y \in G w$ i.e. $g=m y \in M G_{w}$ $\square$

Lemma Let 6 be a quasi-primitive grap. Then 6 has at most two minimal normal subgraps. If $M, N$ are two minimal normal subgraps, then $M \simeq N$, nonabelian, regular. pf: $G$ has at most two minimal normal subgraps - to be proved If $M, N$ are two minimal normal subgraps, $M \cap N=\{1\}$.
So $M \times N=M N \triangle G$ and $M$ and $N$ ammutes.
(1) regular
since $N$ transitive.
For any $w, w^{\prime} \in \Omega, V^{V} \exists x \in N$, s.t. $w^{x}=w^{\prime}$. Then $M_{w^{\prime}}=M_{w^{x}}=x^{-1} M_{w} x=M_{w}$ since $M$ and $N$ commutes. So $M \omega$ fixes all points. $w^{\prime} \in \Omega$. Then $M w=1$. since $M$ faithful. Similarly $N_{w}=413$.
(2) nonabelian

If $M$ or $N$ abelian, WLOG. assume $M$ abelian. Then since $M, N$ commute, $\langle M, x\rangle=H$ for any $x \in N$ is strictly bigger than $M$ and abelian. It is transitive, then it is regular (since $H_{w}=H_{w^{\prime x}}=x^{-1} H w^{\prime} x=H w^{\prime}$ ). But for regular graps $A, \forall g, h \in \Omega, \exists!x \in A$ s.t. $g^{x}=h$.
Then $|A|=|\Omega|$. Since $M$ and $N$ regular, $|M|=|N|=R$ Hence $|H|>|\Omega|$ contractiction. Thus $M$ and $N$ both nonabeliar
(3) $M \simeq N$

Let $X=M \times N \triangle G$.
Then $\quad X=M X_{w}=M: X_{w}$ and $X_{w}=\left\{\left(x, X^{\prime}\right) \mid x \in M, X^{\prime} \in N\right\}$. where $x^{\prime}$ is uniquely determined by $x$
Define $\varphi: M \rightarrow N$

$$
x \mapsto y . \quad \text { if }(x, y) \in X w .
$$

prove (1) $\varphi$ is a homom oxphism

## Week 7

## Lemma

Let $G \leqslant \operatorname{Sym}(\Omega)$ be transitive. Then $C_{\text {sym }(\Omega)}(G)$ is Serniregular.
Pf: Let $C=C \operatorname{Sym}(\Omega)$ (6).
For $w, w^{\prime} \in \Omega, \exists g \in G$ s.t. $w^{g}=w^{\prime}$. Then $C_{w^{\prime}}=C_{w^{g}}= C_{w}{ }^{g}=g^{-1} C_{w} g=C_{w}$.
So $C_{w}=1$ and $C$ is Semiregular.

## corollary

A ransitue abelian permutation grap $G$ is regular. Pf: $G \leqslant \operatorname{Csym}(\Omega)(G)$.

Thm (from last time)
let 6 be a quasi-primitive grap. Then 6 has at most? $\operatorname{Cym}(\Omega)(N)$ semirgantar, O ク $M=C \operatorname{syn}(\Omega)(N)$. two minimal normal subges. If 6 has two minimal normal subgraups $M, N$, then $M \simeq N, M, N$ regular, nonabelian. pf:
(1) $M, N$ regular luse lemma).

$$
X=M N=M \times N
$$

$\Rightarrow M \leq \operatorname{Sym}(\Omega)(N)$
$\Rightarrow M$ is regular, so is $N$.
$e M \simeq N$.
for $w \in \Omega, X_{w}=\left\{\left(x, x^{\prime}\right) \mid x \in M, x^{\prime} \in N\right\} \leq M \times N$.

Let $\varphi: M \rightarrow N$

$$
x \mapsto x^{\prime}
$$

-, surjective $Q$ injective
campano.

Case: assume that 6 has a unique minimal normal subgp $N$.

## Lemma

Let 6 has a unique minimal normal nonabelian subgrapp $N$. Then $G(N)=\{1\}$ so $G \leqslant \operatorname{Aut}(N)$ by NC lemma.
off: Since $N$ is nonabelian. $N \neq(G(N)$. Since $\operatorname{Co(N)} \Delta G$ it would contain a minimal normal subgraup. if $C G(N) \neq\{1\}$, contradiction. Thus $C_{6}(N)=\{1\}$

Thus
(1) If $N=Z^{n}$, then $N$ is regular.
(2) If $N$ is nonabelian simple, then $N \varangle G \leqslant \operatorname{Ant}(N)$
(3) Let $N=T^{*}$ where $k \geqslant 2$ and $T$ is nonabelian simple
(i) $N$ is regular $\Leftrightarrow N_{w}=1$ TW (twisted wreath product).
(ii) $N_{w} \cong T \quad S D_{k}$ (simple diagonal)
(iii) $N_{w} \simeq T^{m} \quad 2 \leq m \leq \frac{k}{2} \quad C D$ (compound diagonal)
(iv) $1 \neq N w \neq T^{m}$ for $m \geqslant 1$. PA (product action).

## Tihm (0'Nan - Scott - Praeger)

Let 6 be a quasi-primitive grap. Then one of the following holds:
() (1) $G$ has exactly two minimal normal subgraps $M \simeq N$ HS (holomoph simple).
(2) 6 has two minimal normal subgraps $M \simeq N \simeq T^{k}$ with $k \geqslant 2$ HC type (holomorph compand). holomoph affic (3) $G$ has anique minimal normal abelian subgrup type. $\mathrm{H}^{-} \mathrm{A}^{-}$deli (4) $G$ has a unique minimal normal subgrap $N$, simple and ( nonabelian. type As (almost simple).
( 5). $G$ has a minimal normal nonabelian regular subgrapp Figenston
(6) (3) (ii) (abve)
(7) (3)(iii)
(8) (3)(iv).

## Case 11)

$6 \leqslant \operatorname{Sym}(\Omega)$. quasi - primitive.
let. $M, N \underset{m, n}{\rightarrow} G$ Then $M \simeq N$. nonabelian. regular. so $M \times N$ $|\Omega|=|M|=N \mid$.
fix $w \in \Omega$. for any $w^{\prime} \in \Omega$. $\exists!x \in M$ s.t. $w^{X}=w^{\prime}$

⇒ There is a $1-1$ correspondance between $\Omega$ and $M$ by

$$
\begin{align*}
w & \mapsto 1  \tag{0}\\
w^{\prime} & \mapsto x^{\prime} . \tag{O}
\end{align*}
$$

Relabe1:

$$
\begin{align*}
& \Omega=\left\{w_{1}, w_{2}, \ldots, w_{n}\right\} \quad \text { where } \quad \Omega \rightarrow M w_{i}+h  \tag{0}\\
& M=\left\{1=x_{1}, x_{2}, \ldots, x_{n}\right\} \quad, \quad w_{i} \mapsto x_{i} w_{i}=w_{1}^{x_{i}}
\end{align*}
$$

$\_\_\_\_$
Lemma relationship between $\hat{g}, \hat{g}, \hat{g}$ cright multiplication/ $/ \mathrm{m} /$ compyight Let $G$ be a finite grap. $f$ ix $g \in G$. define permutations $O$ on $G$ :

$$
\begin{align*}
& \hat{g}: x \mapsto x g  \tag{0}\\
& \tilde{g}: x \mapsto g^{-1 x} \\
& \tilde{g}: x \mapsto g^{-1 x}
\end{aligned} \quad \Rightarrow \begin{aligned}
& \hat{G}=\{\hat{g} \mid g \in G\} \\
& \tilde{G}=\{\tilde{g} \mid g \in G\} \ldots  \tag{0}\\
& \tilde{G}=\{\hat{g} \mid g \in G\} .
\end{align*}
$$

◯
$\_\_\_\_$
(1) $\hat{G}, \quad \hat{G} \leqslant \operatorname{sym}(6), \quad$ regular
(2) $\langle\hat{G}, \vec{G}\rangle=\widehat{G} \vec{G}=\hat{G} \cdot \vec{G}$ contral $\hat{G} \cap \vec{G}=Z(\vec{G})=Z(\vec{G})=2 \widehat{G}(\vec{G})=Z(\vec{G}$
(2) Let $X=\hat{G} \vec{G}, X_{1}=\widetilde{G}$ and $X=\hat{G}: \widetilde{G}=\widetilde{G}: \widetilde{G}$
◯

Def central product ( $M \circ N$ (or $M * N)$ ):

$$
\begin{gathered}
c_{1} \leq Z(M), \quad c_{2} \leq Z(N) \\
c_{1} \simeq c_{2}, c_{1} \times c_{2} \\
c=\left\{\left(c, c^{\varphi}\right) \mid c \in c_{1}\right\} \simeq c_{1}
\end{gathered}
$$

Cappus $N=M \times N / C \rightarrow M_{0} N$ (usually when $C$ is maxibal i.e. $Z(M) \simeq Z(M) \simeq C^{C}$, caled standard c.p.potherice at
example
(1) $Q_{8} \times Q_{8}$
$Z=\{1,-1\}=Z_{2}$
Let $Z=\left\langle C_{1}\right\rangle=\left\langle C_{2}\right\rangle$.
Then $Q_{8} \times Q_{8} /\left\langle\left(C_{1}, C_{2}\right)\right\rangle=Q_{8} \circ Q_{8}$. with $\left|Q_{8} \cup Q_{8}\right|=32$.
(2) Q8: $Z_{4}$
$Q_{8} \circ Z_{4}=Q_{8} \times Z_{4} /\left\langle\left(C_{1}, C_{2}\right)\right\rangle$

$$
\begin{array}{ll}
M=Z_{8}: Z_{2} & a^{z}=a^{5} \\
\langle a\rangle\langle z\rangle & \left(a^{2}\right)^{z}=a^{k}=a^{2}
\end{array}
$$

$a \in Z(M)=\left\langle a^{2}\right\rangle=Z_{4}$.

$$
N=\begin{gathered}
\langle b\rangle:\langle y\rangle \\
8
\end{gathered} \quad b^{y}=b^{5}
$$

$C_{2} \in Z(N)=Z_{4}$
standard-
-If $\quad\left|c_{1}\right|=\left|c_{2}\right|=4$
$M \times N /\left\langle\left(C_{1}, C_{2}\right)\right\rangle=M \cdot N \rightarrow$ central produc
Df $\left|k_{1}\right|=\left|k_{2}\right|=2$
$M \times N / \frac{\left\langle\left(c_{1},(2)\right\rangle\right.}{c}=M{ }_{c} N$
of of lemma:
C (1) $\hat{\sigma}$ is a group as $\hat{g}, \hat{h} \in G \Rightarrow \hat{g} \hat{h}=\hat{g h} \in \hat{\sigma}$
Similarly $\widetilde{G}$ is a grap. $\left(x^{\check{g} \check{h}}=\left(x^{\check{g}}\right)^{\check{h}}=\left(g^{-1} x\right)^{\check{h}}=h^{-1} g^{-1} x=x^{\check{g}}\right.$
Morewer, for any $x, y \in \Omega(\equiv 6), \hat{g}=x^{-1} y$ maps $x$ to $y$ so $\vec{G}$ transitive.
$C \Rightarrow G$ regular since $|\hat{G}|=|6|$ :
( 2) let $\hat{g} \in \hat{G}$ and $\tilde{n} \in \hat{G}$
C Claim: $\hat{g} \check{h}=\bar{h} \hat{g} \in \operatorname{Sym}(\Omega)$.

For $x \in 6$ ，we have

$$
\begin{aligned}
& x^{\hat{g} \tilde{h}}=\left(x^{\hat{g}}\right)^{v}=(x g)^{v}={ }^{-1} \times g \\
& x \breve{h} \vec{g}=\left(x^{\breve{h}}\right)^{\hat{g}}=\left(h^{-1} x\right)^{\hat{g}}=h^{-1} \times g .
\end{aligned}
$$

Hence $\hat{G}$ and $\check{G}$ commute and $\langle\hat{G}, \check{G}\rangle=\hat{G} \check{G}$
Morever，$\hat{G} \mathscr{G}={ }^{\prime} \hat{G} \cdot \hat{G}$ since $\hat{G}$ and ${ }^{\prime}$ commute．
$(\mathcal{Y}: M \times N \rightarrow M N$ is a homomorphism since $\hat{G}$ and $\vec{G}$ commute $(m, n) \mapsto m n$

$$
\begin{equation*}
\operatorname{ker} \varphi=\{(\hat{g}, \vec{g}) \mid, g \in Z(6)\}, \quad \hat{g} \in Z(M) \tag{$)$.}
\end{equation*}
$$

（3）Let $x=\langle\hat{G}, \vec{G}\rangle=\hat{G} \circ \vec{G}$
Then $X_{w}=\{(\hat{g}, \breve{g}) \mid g \in G\}$

$$
x(\hat{g}, \hat{g})=g^{-1} \times g .
$$

where $w=1 \in G$ ．

$$
\hat{G} \text { regular } \Rightarrow \hat{G} \cap X_{w}=1 \& \hat{G} \varangle X
$$

Thus $X_{1}=\widetilde{\sigma}$ and $X=\hat{\sigma}: X_{w}(w=1)=\hat{\sigma}: \widetilde{\sigma} \& X_{2} \hat{\sigma} X_{w}$
So $\widehat{G} \widetilde{G}=\widehat{G}: \widetilde{G}=\widetilde{G}: \widetilde{G}$
（内半直积）．
（内半直积）．
◯

In particular，if $z(6)=1$ ，then $\hat{\sigma} \times \hat{\sigma}=\hat{\sigma}=\tilde{\sigma}=\tilde{\sigma}=\hat{\sigma}$ ，

Go back to case（1）．
Identify $\Omega$ with $M$ ．Then $\hat{M}$ acts regularly on $M$ by right multiplication．
Let $X=M \times N<\operatorname{Sym}(\Omega)$ ．
Then $X_{w}=\left\{\left(g, g^{\prime}\right) \mid g \in M\right\}$
$\varphi: M \rightarrow N$ s．t．$g^{\varphi}=g^{\prime}$
$\Rightarrow X=\hat{M} \times M$ on $M$ ．
try to prove two actions are equivalent．C

Reall $\widetilde{\omega}_{\mapsto 1} \mu$

$$
X_{w}=\left\{\left(g, g^{\prime}\right) \mid g \in M\right\} .
$$

$\left.x_{1}=\{\hat{g}, \hat{g}) \mid g \in M\right\}$. $\varphi: M \rightarrow N$ s．t．$g^{\prime}=g^{\varphi}$
$(g, 1) \mapsto(\operatorname{lig})$
$M \times\{B\} \rightarrow\{B \times M$.
$X=M N=M \times N=M \times M=\hat{M} \times \hat{M} \Rightarrow M \equiv \hat{M}, N \equiv \check{M}$
$Z\left(a^{2}\right)^{3}=1 . M<\operatorname{Sym}(\Omega)$ regular．
$\Rightarrow(\operatorname{sym}(\Omega)(M) \geqslant N$
$\Rightarrow \operatorname{Csym}(\Omega)(M)=N$ by lemma. ( $M$ transitive. $\operatorname{Csym}(\Omega)(M) s . r$. Similarly, $\operatorname{Csym}(M):(\hat{M})=\hat{M}$

## lemmal

Let $M<\operatorname{Sym}(\Omega)$ be regular. Then identifying $\Omega$ with $M_{1}$ the action of $M o n \Omega$ is equivalent to the action of $C \hat{M}$ on $M$.

$$
\begin{aligned}
& \text { Pf: }\left\{\begin{array}{l}
\Omega=\left\{w_{1}, w_{2}, \ldots, w_{n}\right\} \quad w_{i}=w_{1}^{g_{i}} \\
M=\left\{1=g_{1}, g_{2}, \ldots, g_{n}\right\} \\
w_{i}{ }^{g}=w_{1}^{g_{i} g} \rightarrow g_{i} g \\
w_{i} \mapsto g_{i},\left(g_{i}\right)^{g}=g_{i} g
\end{array}>\right.\text { gequivalent }
\end{aligned}
$$

$\square$

Morever, $N=\operatorname{Csym}(\Omega)(M)=\operatorname{Csym}(M)(\hat{M})=\widetilde{M}$

## Week 8 holomorph．

## Corollary

Each regular action of a grapp $x$ is equivalent to $\hat{x}$ acting on $X / x_{\text {艾 acting on }} X$ ．
$p f 1$ we only prove for $X$
Let $\Phi: X \rightarrow \Omega$

$$
\varphi: \begin{aligned}
& x_{i} \mapsto w x_{i}^{-1} \\
& \dot{x}_{v} \rightarrow x \\
& x_{j} \mapsto x_{j} .
\end{aligned} \quad \Rightarrow \quad x_{i}^{x_{j}}=x_{j}^{-1} x_{i} \rightarrow x_{i}^{-1} x_{j}=\left(w^{x_{i}^{-1}}\right) x_{j} .
$$

Then $\Phi$ is $H$ and $\rho$ is is 0 ．
hence two actions are equivalent．

Def holomorph（全形）．
Let $G$ be a group．The holomorph of $G$ is $H o l(G)=G: \operatorname{Aat}(G)$ ．
CHol $(G)=\{(9,6) \mid g \in G, 6 \in$ Ant $(G)\}$ where ◯

$$
\begin{equation*}
\left(g_{1}, b_{1}\right)\left(g_{2}, b_{2}\right)=\left(g_{1} g_{2} 6_{1}^{-1}, 6_{1} b_{2}\right) \tag{).}
\end{equation*}
$$

◯
◯
Case（1）holomorph simple．
$M \times N \triangle G \leq \operatorname{Sym}(\Omega)$ where $M \simeq N \simeq T$ simple

Then $M: \ln n(M) \leqslant G \leqslant M: \operatorname{Aut}(M)$
pf ：
LHS：$\hat{M} \times \hat{M}=M \times N \triangleleft G$

$$
\begin{aligned}
& \hat{M}^{\prime \prime}: \widetilde{M} \\
& M: \operatorname{Inn}(M)
\end{aligned}
$$

RHS：$G=M: G w \leq \operatorname{Sym}(\Omega)$ ．
identify．$M$ with $\Omega$ ，we have

Campus

$$
G=\widehat{M}: G_{1} \leq \operatorname{Sym}(M) .
$$

Claim: $G_{1} \leq A_{n}(M)$.
$\forall 6 \in G_{1} ; G^{-1} \hat{m} 6=\hat{m}^{\prime}$ for sume $m^{\prime} \in M$.
Then $6^{-1} \hat{m} 6(1)=\hat{m}(1) \Rightarrow 6(m)=m^{\prime}$
Let $6^{-1} \hat{m}_{0}=\hat{m}^{\prime} ; 6^{-1} \hat{n} 6=\hat{n}^{\prime}$. Then $6^{-1} \hat{m} \hat{n} 6=\hat{m}^{\prime} \hat{n^{\prime}}$ which yields $6(m n)=m^{\prime} n^{\prime}=6(m) 6(n)$. Hence $G_{1} \leq A n t(m)$.

Lemma (Frattini argument)
Let $H<G \leqslant \operatorname{Sym}(\Omega)$ s.t. $G$ is transitive on $\Omega$.
Then $H$ transitive on $\Omega \Leftrightarrow G=H G w$ (i.e. $\forall g \in G, \exists x \in H$, as set. $\quad y \in b_{w}$ s.t. $g=x y$ ).

Lemmal Let $M \& G \leqslant \operatorname{sym}(\Omega)$ s.t. $M$ is regular on $G$. Then $G=M: G w$ where $w \in \Lambda$.

Case (2) holomorph compound.
$M \times N \varangle G \leqslant \operatorname{Sym}(\Omega)$
Where $M \simeq N=T^{\ell}$ with $\ell \geqslant 2$ and $T$ honabelian simple Then $M: \widetilde{M} \& G \leqslant M: \operatorname{Aut}(M)$ where $\operatorname{Aut}(M)= \operatorname{Aat}(T) \sim S_{l}$ lwreath product).
$C M \simeq N=T^{\ell}=T_{1} \times \cdots \times T_{l}$ where $T \simeq T_{1} \simeq \cdots \simeq T_{l}$ 6 induces a transitive action on $\left\{T_{1}, \cdots, T_{l}\right\}$.)

## Lemma

C A quasiprimitive permutation group of type HS.HC. or C HA is primitive.

Let 6 be a transitive permutation grap on $\Omega$.
Def block \& block system.
(1) A subset $B \subset \Omega$ is called a block if for any element $g \in G, \quad B^{9}=\left\{w^{9} \mid w \in B\right\}$ either $B^{9}=B$ or $B^{9} \cap B=\phi$ (2) For a block $B$, the set $B=\left\{B^{g} \mid g \in G\right\}$ is called a block system for $G$ on $\Omega$.

## properties of blocks

Write $B=\left\{B_{1}, B_{2}, \cdots, B_{m}\right\}$.
(1) each Bi is a block.
(2) $B i \cap B j=\phi \quad \forall i \neq j$.
(3) $\left|B_{1}\right|=\cdots=\left|B_{m}\right|$
(4) $G$ induces a transitive action on $\beta$.

## Def primitive \& inprimitive

If there is a block $B$ for $G$ on $\Omega$ with $1<|B|<\xi$ (i.e. nontrivial block $B$ ). Then $G$ is said to be imprimity on $\Omega$. Otheruise $G$ is said to be primitive.

Lemma A transitive grap $G \leqslant \operatorname{sym}(\Omega)$ is primitive $\Leftrightarrow G_{w}$ is a maximal subgrup of 6 .
of:
⟸ Assume otherwise $G$ is imprimitive on $\Omega$, let $B$ be a nontrivial block. Let $w \in B$, then

$$
G_{w} \neq G_{B}=\left\{g \in 6, \quad B^{g}=B\right\} \text {. }
$$

$\left(\exists w^{\prime} \neq w, \in B, \exists g \in G\right.$ s.t. $w^{g}=w^{\prime}$ then $g \in G_{B}$ but not $\left.G_{w}\right)$.
Campus
i.e. $G_{N} \neq G_{B} \neq G$

⇒ If $G_{w}<H<G$. then $B_{:}=w^{H}$ is a block.
(1) $B^{g} \cap B=\phi$ or $B^{g}=B$.

C let $g \in \sigma$. Consider $B^{g} \cap B$. Naw suppose $\delta \in B^{g} \cap B$.
Then $w^{h_{1} g}=w^{h_{2}}$ for some $h_{1}, h_{2} \in H$ so $h_{1} g h_{2}{ }^{-1} \in G w<1$ Since $h_{1}, h_{2} \in H, g \in H$, hence $B^{g}=B$.
(2) $B$ is a nontivial block.
ase remma. if $H$ transitive, Then $G=H G_{w}=H$. Hence
$C H$ not transitive and $|\angle| B|<|\Omega|$. $\square$
of of lemma: Case HA.
let $G$ be of type HA. Then $G=M: G_{w}$ where $M$ is abelian unique minimal normal subgraup.

Suppose 6 is imprimitive on $\Omega=M$. Then ${ }^{2} \dot{G}_{w}<H<G$. and I $\neq H \cap M=M_{0}<M$. (if $H \cap M=1$. Then $G \notin M \cdot H$ ).
Then $M_{0} \varangle M_{1} M_{0} \varangle H, M_{0} \forall\langle H, M\rangle=6$, antradiction Mabelian

Counterexample example of being quasi-primitive but not primitive (note that prinifive → quasiprimitive:
pf: let $\quad H \triangleleft G$
(1) $H<G_{\omega}$ for some $\omega \in \Omega$.

Then $H<G \omega^{x}=G \omega^{x}$ for any $x \in G$.
i.e. $H$ fixes any element in $\Omega$ since $G$ is transitive $\Rightarrow H=1$ since 6 faithful
C (2) $H \neq 6 w$ for any $w \in \Omega$.
C Then. HGw is a graip strictly larger that $6 w \Rightarrow 6=H G_{w}$
$C\left(\begin{array}{l}h_{1} g_{1}\left(h_{2} g_{2}\right)^{-1}=h_{1} g_{1} g_{2}^{-1} h_{2}^{-1}=\underbrace{h_{1} g_{1} g_{2}^{-1} h_{2}^{-1} g_{2} g_{1}^{-1}}_{e \in H G_{w} .} \underbrace{g_{1} g_{2}^{-1} \in H G_{w}}_{\left.v h_{1}, h_{2} \in H, g_{1}, g_{2} \in G_{w}\right)} \Rightarrow\end{array}\right.$

Let 6 be non abelian simple.
Then $\hat{G}$ is a quasi-primitive grap on $G$. but $\hat{G}$ is not primitive since $\hat{G}_{\omega}=1$ is not maximal: in $\hat{G}$.

## Week 9

HS type (HC type * (homework).
Let $G=T: H$ with $\ln n(T) \leqslant H \leqslant \operatorname{Aut}(T)$

- $H$ can be $G w$ with $w=1 \in T$ corany other $\left.w^{\prime}\right)$.
- Claim: $G$ is primitive on $\Omega(\equiv T)$ ( $\Leftrightarrow G_{w}$ is maximal of: Suppose $H \leqslant K \leqslant G$.
Then an elt $g \in K \backslash H$ has the form $g=(t, h) \in G=T: H$ such that $t \neq 1$ thus $(t, 1)=(t, h)\left(1, h^{-1}\right) \in K$.
For any elt $x \in H$, we have $\left(1, x^{-1}\right) \in K$ and $\left(1, x^{-1}\right)(t, 1) \in \left(t^{x}, x^{-1}\right)(1, x)=\left(t^{x}, 1\right) \in K$.
Let $M=\left\langle\left(t^{x}, 1\right) \mid x \in H\right\rangle \leqslant T \times\{1\}$.

Since $H \geqslant \operatorname{Inn}(T)$, the action of $T$ on $M$ by conjugation fixes $M$, so $M \Delta T$. Since $T$ simple, $\langle M, H\rangle=\left\langle T, H_{1}\right\rangle =T: H=G \leqslant K$. So $H$ is a maximal subgrap of $G$ and $G$ is primitive on $\Omega$.

Corollary This provides a way to construct primitive graps For $T$ simple, let $G=T \times T=T: \widetilde{T}=T: \ln n(T)$ (since $T$ simple, $Z(T)$ is trivial, hence semidirect product).

## Notation

let $G \leqslant \operatorname{Sym}(\Omega),|\Omega|=n$

- For $\Delta \leqslant \Omega$ and $g \in \sigma$, let $\Delta^{g}=\left\{\delta^{g} \mid \delta \in \Delta\right\}$.
- For $w_{1}, w_{2} \in \Omega$, write $w_{1} \sim w_{2}$ if $w_{1}^{g}=w_{2}$ for some $g \in G$.
$C-w^{G}=\left\{w^{9} \mid g \in G\right\}$ orbit of $w$.
$C=G w=\left\{g \in G\left\{w^{g}=w\right\}\right.$ stabilizer of $w$.


## 44

◯

- For $\Delta c \Omega$, let $G \Delta=\left\{g \in G \mid \Delta^{g}=\Delta\right\}$ ( $=G\{\Delta\}$ )

$$
\begin{equation*}
G(\Delta)=\left\{g \in G \mid w^{g}=w^{\prime} \forall w \in \Delta\right\} . \tag{O}
\end{equation*}
$$

$\Rightarrow G(\Delta) \Delta G \Delta$ and $G \Delta$ acts on $\Delta$ with kernel $=G(\Delta) O$

- $g^{-1} 6 w g=6 w^{9}$ ◯
◯
$g^{-1} \sigma_{\Delta} g=\sigma_{\Delta}{ }^{9}$
$g^{-1} G(\Delta) g=G\left(\Delta^{9}\right)$
- $G \Delta$ acting on $\Delta$ induces a permutation grapp an $\Delta$, denoted by $G_{\Delta}^{\Delta}$ and $G_{\Delta}^{\Delta} \simeq G_{\Delta} / G(\Delta)$.
— Fix $_{\Omega}(6)=\left\{w \in \Omega \mid w^{9}=w \quad \forall g \in G\right\}$.
The set of fixed points of $G$ on $\Omega$.
◯
- $F i x_{\Omega}(g)=\left\{w \in \Omega \mid w^{g}=w \quad \forall g \in 6\right\}$.
◯


## Def semiregular

$G$ is said to be semiregular on $\Omega$ if $G_{w}=\{1\}$ for
◯ O each point $w \in \Omega$.

- $1 G 1=$ length of each orbit $\Rightarrow|G|||\Omega|$ and orbit length $|$
- If $G$ is transitive and semiregular, then $G$ is regular on ( $\Omega$ and $|G|=|\Omega|$

Def coset action. ( $G$ transitive on $\Omega$ ).

Let $G$ be a grap and $H \leqslant G$. Let $\Omega=[G: H]=$
$\{\mathrm{Hg} \mid g \in 6\}$. Define an action of 6 on $\Omega$ by

$$
(H g) X=H(g x) \in[G: H] \quad \forall H g \in \Omega \text { and } x \in G
$$

i.e. $\quad x: \mathrm{Hg} \mapsto \mathrm{Hgx}$

- A coset action is transitive $\left(\left(H g_{1}\right)^{g_{1}^{-1} g_{2}}=H g_{2}\right)$.
- The kanel of this action is $G(\Omega)=\bigcap_{g \in G} g^{-1} \mathrm{Hg}$.

Denote $G(\Omega)$ by $M$. Then $M=\bigcap_{g \in G} g^{-1} H g \triangle G, M \leqslant H$ and $M$ is the largest normal subgrap $g \in \sigma$ of $G$ contained int called the core of $H$ in $G$... denoted by Core $(H)$.
prop $G /$ core $_{G}(H) \simeq G^{\Omega}$ is a ransitive permutation group on $\Omega=[6: H]$.

- If $\operatorname{Core}_{6}(H)=1$, then the action is said to be faithful. ( $H$ is core-free in 6 ). Otherwise the action is unfaithful.


## Thm

Each transitive permutation grup is equivalent to a faithfi coset action. ${ }^{1}$ faithful.
pf:
Let 6 be a transitive permutation graup on $\Omega$ i.e. $G \leqslant$ Symbe is transiture.
let. $H=G w$ with $w \in \Omega$. let $\Sigma=[G: H]$. Then for $6=\mathrm{Hg}$ and $x \in 6,6^{x}=\left(\mathrm{Hg}_{9_{2}}\right)^{x}=\operatorname{Hgx} \in \Sigma$.
$\left\{\begin{array}{llll}\Omega: & w, w^{g_{1}} ; w^{g_{2}}, & \ldots, & w^{g_{n}} \\ \Sigma: & H_{1} & H g_{1}, H g_{2}, & \ldots,\end{array}\right.$
For $x \in G$, s.t.

$$
\begin{aligned}
x: w^{g_{i}} & \rightarrow w^{g_{j}} \\
\mathrm{Hgi} & \rightarrow \mathrm{Hgj} .
\end{aligned}
$$

- $H$ is core-free in 6. i.e. Core G $(H)=1$ i.e. $H$ does not contain a nonidentity normal subgrapp of $G$.
Pf: Suppose $N \leq H$ and $N \triangleleft G$.
C Then $N=N \cdot N$ sina $N \leq H$. for $x \in 6, N w=N=X^{-1} N X$
$=x^{-1} N_{w} x=N_{w} x$ so $N$ fixes every point of $\Omega$.

We then must have $N=1$ as $G \leq \operatorname{Sym}(\Omega)$. Thus Core ${ }_{6}(H) =1$ and $G$ acts on $\sum$ faithfully.

Def equivalence of action. (same G)
Let $G$ acts on $\Omega$ and $\Sigma$ transitively and faithfully. Then $G^{\Omega}$ and $G^{\Sigma}$ are said to be equivalent if there is a $H \operatorname{map} \varphi$ from $\Omega$ to $\Sigma$ s.t. for any $w \in \Omega$,
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-46.jpg?height=297&width=699&top_left_y=850&top_left_x=387)

Example equivalent or not

$$
\begin{aligned}
G & =S_{4} \text { on }\{1,2,3,4\} \\
H_{1} & =\langle(12)\rangle \\
H_{2} & =\langle(12)(34)\rangle \\
H_{3} & =\langle(13)\rangle
\end{aligned} \Rightarrow \Omega_{1}=\left[0: H_{i}\right] \Rightarrow G^{\Omega_{1}} \text { and } G^{\Omega_{2}} \cdot X_{i}
$$

Pf:
$D G^{\Lambda_{1}}$ and $G^{\Omega_{2}}$ : note that $H_{1}(12)=H_{1}$
If... $H_{1} \rightarrow H_{2} g$ for some $g \in G$. then we must have $\mathrm{H}_{2} \mathrm{~g}=\mathrm{H}_{2} \mathrm{~g}$ (12) which means

$$
1^{0} \begin{cases}g(12)=g & 2^{\circ} g(12)=(12)(34) g \\ (12)(34) g(12)=(12)(34) g & (12)(34) g(12)=g .\end{cases}
$$

both not possible.
(2) $G^{\Omega_{1}}$ and $G^{\Omega_{2}}$

By_above $1^{\circ}$ and $2^{\prime \prime}$ first row is some conjugation relation? Frenens we may take $\mathrm{H}_{1} \rightarrow \mathrm{H}_{3}(23)$.

So $\varphi:\left[G: H_{1}\right] \rightarrow\left[6: H_{3}\right]$

$$
\begin{array}{ll}
H_{1} & H_{1} g \\
H_{3}(23) & H_{3}(23) g \tag{23}
\end{array}
$$

only need to check $\varphi$ is well-defined.
Indeed if $H_{1} g_{1}=H_{1} g_{2}$ then $g_{1} g_{2}^{-1} \in H_{1}=H_{3}$

Hence $(23) H_{3}(23) g_{1}=(23) H_{3}(23) g_{2} \Rightarrow H_{3}(23) g_{1}=H_{3}(23) g_{2}$.
Thm criterion for equivalence
$G^{\Omega}$ and $G^{\sum}$ both transitive are equivalent仙
$G_{W}$ and $G_{6}$ are conjugated in $G$.
pf:
⇐ defined map is well-defined
$\Rightarrow \Omega \rightarrow \Sigma: g^{-1} G_{6 g} \subset G_{w}$
$\Sigma \rightarrow \Omega: g^{-1} G_{w} g^{\prime} \subset G_{6}$.

Week 10.
Exercise 13
Characterize quasiprimitive permutation grapp $\sigma \leqslant \operatorname{sym}(\Omega)$ such that $G_{w}$ has $r$ orbits on $\Omega$ where $r=2,3,4,5$. If $G$ is of holomorph simple type, then $r \geqslant$ ?

## Thm (Burnside)

A 2 -transitive permutation grapp is a ffine (holomouph affine) or almost simple (AS) $\quad \forall \operatorname{rank}(0)=2$.

Def $\operatorname{rank}(6)$
Let $6 \leqslant{ }^{\circ} \operatorname{Sym}(\Omega)$ be transitive. Then $G$ induces an action on $\Omega \times \Omega=\left\{\left(w_{1}, w_{2}\right) \mid w_{1}, w_{2} E \Omega\right\}$ by
$\left(w_{1}, w_{2}\right)^{g}=\left(w_{1}^{g}, w_{2}^{g}\right) \quad \forall$ adered. ◯
Then $G$ acts intransituely and partitions ' $\Omega \times \Omega$ into $G$-orbits.

The number of $G$-orbits is called the rank of $G$ on $\Omega$, denoted by $\operatorname{rank}(6)$.

- $\{(\omega, \omega) \mid \omega \in \Omega\}$ is an orbit. so $\operatorname{rank}(6) \geqslant 2$
◯
If $\operatorname{rank}(6)=2$, then $G$ is called 2-transitive
- A 2 -trans group is primitive.
- If $G$ is 2 -transitive on $\Omega$. Then
(i) $6 w$ is faithful and transitive on $\Omega \backslash\left\{S_{w}\right\}$.
(ii) $|\Omega|-1$ divides $\left|\sigma_{w}\right|$
(iii) If $N \varangle G$, then $N_{w}$ is $\frac{\text { half-transitive on } \Omega \backslash\{\omega\} \text {. }}{\downarrow}$ on
orbits have the same size.

If: (iii) use the following lemma with

$$
\begin{array}{r}
N_{w}=N \cap G_{w} \& G_{w} \text { where } G_{w} \text { is transitive in } \\
\Omega \backslash\{w\} .
\end{array}
$$

Lemma $G$ transitive, $N \triangleleft G \Rightarrow N$ half-transitive. Let $G \leq \operatorname{Sym}(\Omega)$ be transitive. $1 \neq N<G$ intransitive. Then the $N$-orbits on $\Omega$ form a nontrivial block system and $N$ is half-transitive.
pf: Let $\Delta_{1}, \Delta_{2}$ be two $N$-orbits. $s_{i} \in \Delta_{i}$ with $i=1$ or 2 Then $\delta_{1}^{g}=\delta_{2}$ for some $g \in G$.
Write $\Delta_{1}=\left\{\delta_{11} \delta_{1} x_{2}, \cdots, \delta_{1} x_{t}\right\}$ with $x_{i} \in N$.
Then $\left(\delta_{1}^{x_{i}}\right)^{g}=\delta_{1}^{g g^{-1} x_{i} g}=\left(\delta_{1}^{g}\right)^{g^{-1} x_{i} g}=\delta_{2}^{g^{-1} x_{i} g} \in \Delta_{2}$.
So $\Delta_{1}^{9} \leq \Delta_{2}$. Similarly $\Delta_{2}^{9} \leq \Delta_{1}$ so $\left|\Delta_{1}\right|=\left|\Delta_{2}\right|$.
Corollary If 6 is primitive, then 6 is quasiprimitive.

## of of thm

let $G \leqslant \operatorname{syn}(\Omega)$ be 2 -transitive. Let $N<6$ s.t. $N=T^{k}$ wit $k \geq 2$ and: $T$ nonabelian simple.

## Lemma 1

$N$ does not have a proper normal subgrapp which is regular on $\Omega$ (i.e. $G$ is not HS. HC.SD.CD).
pf:
(Suppose $M \leqslant N$ is regular on $\Omega$. Then $|\Omega|=|M|$ and $N_{w} \neq 1$ (since $M \leqslant N$.) is half-transitive on $\Omega \backslash\{w\}$. Let $m$ be the size of an orbit of Nw on $\Omega \backslash\{\omega\}$. Then $m\left||\Omega| s_{w}\right\} \mid$ and $m||\mathrm{Nw}|$. Thus $m| \operatorname{gcd}( \left.|n|-1,\left|N_{w}\right|\right)$. Now $|\Omega|=|M|=\prod^{l} \mid$ for $l<k$. and $N= M: N_{w}$ so that $\left|N_{w}\right|=|T| k-l$ Thus $m \mid \operatorname{gcd}\left(\left.\pi\right|^{l}-1\right.$, $\left.\pi 1^{k-1}\right)=1 \Rightarrow m=1$ contradiction. ( $N_{w}$ is faithfu! on $\Omega \mid\{\omega\}$ )

## Lemma 2

$N$ is not regular on $\Omega$. (i.e. $G$ is not $T W$ )

Pf:
:
Suppose $N$ is regular on $\Omega$. Then $G=N \sigma_{w}$ and $N \cap \sigma_{w}=N_{w}=1$ hence $G=N: \sigma_{w}$. and $\Omega$ can be identified with $N$ with $W \rightarrow 1$ s.t. $G w$ acts on $N$ by conjugation. $\left(w^{n} \mapsto n ; w^{n g}=w^{-g g^{-1} n g}=w^{-1} n g_{1} \mapsto \dot{g}^{-1} n g=n^{g-}\right)$. The action of $\sigma_{w}$ partitions $N$ into orbits and fixes 1.
◯
Note that elts of $N$ in the same orbits are of same order.

Clearly $|N|$ has at least 3 prime divisurs $r, s, t$.
( $N=T^{k}$ where $k \geqslant 2, T$. nonabelian simple is not solvable
◯
while graps of odd order are solvable, graps of order $p^{a} q^{b}$ ◯ are solvable by Burnside). Then $\exists x, y, z \in N$ s.t. $|x|=r,|y|=s$, $|z|=t$. Thus $x^{6 w}, y^{6 w}, z^{6 w}$ are 3 distinct orbits O of $G_{w}$ on $N$. Since $\operatorname{rank}(6)=\#$ of $G_{w}$ orbits on $\Omega$, we have $\operatorname{rank}(6) \geqslant 4$. a contradiction.

Remark example of ${ }^{\prime}=4$ " holds: If $N=T=A_{S}$, then $(r, s, t)=2,3,5 . G_{w}=S_{5} \quad \operatorname{rank}(6)=4 . \quad$ (HS) .

## Lemma 3

Let $G$ be 2 -transitive and let $N \underset{\text { min }}{4} G$ such that O $N=T^{k}$ does not have a normal subgrap which is regular on with $k \geqslant 2$ and $T$ nonabelian simple $\Leftrightarrow$ Now $N$ is the 0 unique minimal normal subgrup). Then $\underbrace{}_{\zeta_{2} \pi^{4}}=\Delta^{k} s_{i_{k} \pi_{-1}} G$ acts on $\Omega$ by $\left.\left(\delta_{1}, \cdots, \delta_{k}\right) Z_{1}, \cdots \cdot 2 k\right) \pi=\left(\delta_{1}^{2, \pi^{-1}}, \cdots, \delta_{k}^{2 \pi^{-1-1}}\right)$ (ungrys call type $P A(G$ acts on $\Omega$ in product action)).

Pf: $N_{w} \neq 1$. Write $N=T_{1} \times \cdots \times T_{k}$ where $T_{1} \simeq \cdots \simeq T_{k}$ Let $H_{i}$ be the projection image of $N_{w}$ into $T_{i}\left(g \in N_{w}<\right. T_{1} \times \cdots \times T_{k}, g=\left(t_{1}, \cdots, t_{k}\right)$ where $t_{i} \in T_{i} \mapsto t_{i}$ or $\left.\left(1,1, \cdots, t_{i}, 1, \cdots, 1\right)\right)$ Let $M_{i}=\prod_{j \neq i} T_{j 3}, \quad M_{i} \triangleleft N$ and $N / M_{i} \approx 7$ Then $H_{i}=N_{w} / N_{w} \cap M_{i}=N_{w} M_{i} / M_{i}$. and $H_{i} \leq T_{i}$ with $H_{1} \simeq H_{2} \simeq \cdots \simeq H_{k}$ if not, any orbit is a proper smaller nor mal subgrap ( $G=N G_{w}$ transitue on $\left\{T_{1} \ldots, T_{k}\right\}\left(T_{i}^{g} \Delta N \Rightarrow T_{i}^{g}=T_{j}\right.$ for some $j \quad \forall g \in G$ ) and $N$ trivial on $\left\{\pi, \cdots, T_{k}\right\}$ by conjugation so Gw transitive on $\left\{T_{1}, T_{2}, \cdots, T_{k}\right\}$. Also, Gw normalizes. $N_{w}$ so $G_{w}$ is transitive on $\left\{H_{1}, \ldots, H_{m}\right\}$ by . conjugation).
Then $N_{w} \leqslant H_{1} \times \cdots \times H_{k} \leqslant N$ and $K$ is normalized by $O_{w}$ In transituve.
Thus $G_{w} \leqslant k G_{w}<G$. Since $G$ primitive, $G_{w}$ maximal Then $G_{w}=k G_{w} \Rightarrow k=N w \leqslant G_{w} \quad\left(k \leqslant N\right.$ and $k \leqslant G_{w}$

$$
\uparrow K G_{w} \neq G \text {. Otherwise } K \triangleleft G \text { But } K<N_{\text {a }}
$$

Consider $N=\pi \times \cdots \times T_{k}$ acts on $\Omega$.
Identify $\Omega$ with $[N: N w]$

$$
\begin{aligned}
\Omega=[N: N w] & =\left[\begin{array}{ccc}
T_{1} \times \cdots \times T_{k} & : H_{1} \times \cdots \times H_{k}
\end{array}\right] \\
& =\left[\begin{array}{ccc}
T_{1}: H_{1} & \cdots \\
\Delta_{1} & \cdots & \times T_{k}: H_{k}
\end{array}\right]
\end{aligned}
$$

with $N$ acting on $\Omega$ defined by

$$
\begin{aligned}
w\left(t_{1}, \cdots, t_{k}\right) & =\left(H_{1} x_{1}, \cdots, H_{k} x_{k}\right)^{\left(t_{1}, \cdots, t_{k}\right)} \\
& =\left(H_{1} x_{1} t_{1}, \cdots, H_{k} x_{k} t_{k}\right)
\end{aligned}
$$

$G_{w} \leqslant\left(L_{1} \times \cdots \times L_{k}\right): S_{k}$ where $H_{i} \leqslant L_{i} \leqslant \operatorname{Aut}\left(T_{i}\right)$

$$
\begin{gathered}
G \leqslant\left(A_{n}+\left(T_{1}\right) \times \cdots \times A_{n}+\left(T_{k}\right)\right): S_{k}=\left(T_{1} \times \cdots \times T_{k}\right):\left(O_{n}+\left(T_{1}\right) \times\right. \\
\left(C_{6}(N)=1\right) .
\end{gathered}
$$

$g=\left(Z_{1}, \cdots, Z_{k}\right) \pi$ where $T_{i} \in \operatorname{Aut}\left(T_{i}\right), \pi \in S_{k}$.
$\Rightarrow w=\left(\delta_{1}, \ldots, \delta_{k}\right) \in \Delta_{1} \times \ldots \times \Delta_{k}, \quad g=\left(\tau_{1}, \ldots, \tau_{k}\right) \pi$.

$$
\begin{align*}
\Rightarrow w^{g} & =\left(\delta_{1}, \cdots, \delta_{k}\right)\left(z_{1}, \cdots, z_{k}\right) \pi \\
& =\left(\delta_{1} z_{1}, \cdots, \delta_{k}^{z_{k}}\right)^{\pi} \\
& =\left(\delta_{1}^{z_{1} \pi-1}, \cdots, \delta_{k \pi-1}^{z_{k \pi-1}}\right) \in \Delta^{k} \tag{口。}
\end{align*}
$$

Example

$$
\begin{aligned}
& G=\left(A_{n}+(T) \times A_{n}+(T)\right): S_{2} \\
& \Omega=\Delta_{1} \times \Delta_{2}=\Delta^{2} \\
& G_{w}=\left(L_{1} \times L_{2}\right): S_{2} \quad\left(H_{i} \leq L_{i} \leq A_{n}+(T i)\right.
\end{aligned}
$$

e.g.
$\Delta=\{0,1,2,3,4\}$.
$\Omega=\Delta^{2}=\{(i, j) \mid 0 \leq i, j \leq 4\}$.

Fix $\omega=(0,0)$.

$$
\begin{array}{r}
G_{w}=(\operatorname{Alt}(1,2,3,4) \times \operatorname{Alt}(1,2,3,4)): \pi \\
\left(\pi:\left(t_{1}, t_{2}\right) \rightarrow\left(t_{2}, t_{1}\right)-\right)
\end{array}
$$

Then Gw partitions $\Omega \backslash\{\omega\}=\Delta^{2} \backslash\{(0,0)\}$ into two orbits.
◯ $\{(0, i),(i, 0) \mid 1 \leq i \leq 4\}$ $\square$ $\{(i, j) \quad \mid 1 \leq i, j \leq 4\}$.
$\left((0,1) \xrightarrow{((12)(2 A),(12)(34)+2)}(0,2) ;(0,1) \xrightarrow{\pi}(1,0) ;(1,1) \xrightarrow{\left.(1,1)\left(k_{1}, k_{2}\right),(1 j)\left(k_{1}^{\prime} k_{2}^{\prime}\right)\right)}(1, j) \cdot(1)\right.$ so $\operatorname{rank}(6)=3$. $\_\_\_\_$
lemma (HW)
$G=(T \times T): S_{2}$ acting on $\Omega=\Delta^{2}$ is of rank at least $O_{3}$ and $\operatorname{rank}(6)=3 \Leftrightarrow T$ is 2-transitive on $\Delta$

## problem

Let $G=(T \times T \times T): S_{3}$ acts on $\Omega=\Delta^{3}$ in productactio Find a sharp lower bound for rank ( 6 ) ("" may hold) $\operatorname{rank}(6) \geqslant 4 \Leftrightarrow T 2$ transitive.
clue:

$$
\begin{aligned}
& \Delta=\{0,1, \cdots, n-1\} \\
& \Omega=\Delta^{k}=\left\{\left(\delta_{1}, \cdots, \delta_{k}\right)\right\} . \\
& \omega=(0, \cdots, 0) .
\end{aligned}
$$

If $w \in \Omega$, let weight $(w)=\#$ of $\delta_{i} \neq 0$.
Then same weight ⇒ same orbit).

## Week 11

$\operatorname{rank}(6)=3$

Timm If $\operatorname{rank}(G)=3$ and $G$ is quasi - primitive on $\Omega$, then $G$ is $H A$, AS or $N=T^{2}$ and $G$ acts on $\Omega=\Delta^{2}$ in product O action with $T$ on $\Delta$ is 2 -transitive.
counter example $G=A_{5}: S_{5}=H_{0} \|\left(A_{5}\right) \operatorname{rank}(4)$. "Aut (As).
(As acts on As by right multiplication.
$S_{S}=$ Gw acts on $A_{S}$ by conjugation. (proved below in.lemma 1) identify" $w$ with 1
Then orbits of $G_{1}$ is conjugacy classes of $A_{5}$ under $S_{5}$ :
$O_{1}=\{1\}, \quad O_{2}=\{(i j)(k l)\}, \quad O_{3}=\{(i j k)\}, \quad O_{4}=\{(i j k l m)\}$.

## Lemma 1

Let $M G . G$ be regular on $\Omega$. Then $\operatorname{rank}(G) \geqslant$
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-54.jpg?height=100&width=1426&top_left_y=1285&top_left_x=292) \# of fusion classes of elts of $M$.
✓ orbits under Aut $(M)$

In particular, if $M=T^{k}$ with. T nonabelian simple, then $\operatorname{rank}(G) \geqslant 4$. $(\Rightarrow G$ is not of type $H S, H C, T W)$.

Pf:

Identify $\Omega$ with $M$. et $w \equiv 1$. Then $G=M: G w$ and

Gov acts on $(M \equiv \Omega)$ by conjugation...
( $G$ acts on $\Omega=[G$ : Gw] $=\{$ Gw $g \mid g \in G\} h \in \sigma_{w}$ : $\sigma_{w} g \xrightarrow{h}=\sigma_{w} g h=\sigma_{w} h^{-1} g h=\sigma_{w} g^{h}$

Let $g$ be in $M \Rightarrow g^{n} \in M$ ).

Now the action of $6 w$ on $M$ partitions into obits i.e. fusion
classess of $M$ since $G_{w} \leqslant \operatorname{Aut}(M)$. So $\operatorname{rank}(G) \geqslant \#$ of fusion
classes of $M$.
Suppose $M=T^{k}$ with $T$ nonabelian simple $\Rightarrow M$ not solvable $\left(|M| \neq p^{a} q^{b}\right)$. Then there exists distinct primes $r, s, t$ dividing $|M|$. Let $x, y, z \in M$ s.t. $|x|=r,|y|=s$. $|z|=t$. Then $x^{6 w}, y^{6 w}, z^{6 w}$ are three different fusion classes of $M$. so $\operatorname{rank}(6) \geqslant 4$.

Lemma 2
Let $G$ be a quasi-primitive of type SD. (simple diagonal). Then $\operatorname{rank}(6) \geqslant 4$
pf: Let $N=T^{k} \triangleleft G$ and write $N=T_{1} \times \ldots \times T_{k}$ where $T_{1} \simeq \ldots$ c
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-55.jpg?height=143&width=1637&top_left_y=1122&top_left_x=142)
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-55.jpg?height=164&width=1617&top_left_y=1170&top_left_x=156) is regular on $\Omega \quad(N=R N \omega$ is transitive $\rightarrow R$ transitive $)$. Identify $\Omega$ with $\left[N_{:} N_{w}\right]=\left\{N_{w}\left(t_{1}, \cdots, t_{k}\right) \mid t_{i} \in T\right\}$. where $N_{w}\left(t_{1}, \cdots, t_{k}\right)=\left\{\left(s_{1}, \cdots, s t_{k}\right) \mid s \in T\right\}$ with a representative $\left(t_{k}^{-1} t_{1}, \cdots, t_{k}^{-1} t_{k-1}, 1\right) \in R$
Observe $N_{w}\left(s_{1}, \cdots, s_{k-1}, 1\right)=N_{w}\left(t_{1}, \cdots, t_{k-1}, 1\right) \Leftrightarrow \left(s_{1}, \cdots, s_{k-1}, 1\right)=\left(t_{1}, \cdots, t_{k-1}, 1\right) \quad\left(\left(s_{1}^{-1} t_{1}, \cdots, s_{k-1}^{-1} t_{k-1}, 1\right) \in N_{\omega}\right)$ Now, the stablizer $G_{w}$ satisfies $N_{w} \cdot P \leqslant G_{w} \leqslant \operatorname{Aut}(T) \times S_{k}$ where $P \leqslant S_{k}$ is transitive. ( $G_{w}$ acts by conjugation: $N_{w} \leqslant G_{2}$ Gu acts on Ti transitively and acts faithfully on $N$ as $\operatorname{Co}(N)<G$ and $\operatorname{Co}(N) \cap N=\left\{13 \Rightarrow \operatorname{Coc}(N)=\{1\} . G_{w} \leqslant\right.$ Autc $\times S_{k}$ as Gustabilizes. NW. and Aut $(T) \times S_{k}$ is a direct product as $\operatorname{Aut}(T) \times \cdots \operatorname{Aut}(T)$ becomes $\operatorname{Ant}(T)$ thus Skacts on AutcT frivally).

Each element of $6 w$ has the form $6 \pi$ with $b \in A_{n+(T)}$ and $\pi \in S_{k}$ and an elt $\left(t_{1}, \cdots, t_{n}\right) \in N$ is sent by $6 \pi$ to $\left(t_{1}, \cdots, t_{k}\right)^{6 \pi}=\left(t_{1}^{6}, \cdots, t_{k}^{6}\right)^{\pi} \in N$.
Pick two eltis $x:=\left(s_{1}, 1, \cdots, 1\right), y:=(t, 1, \cdots, 1) \in R \backslash\{(1, \cdots, 1)\}$.

Then $x, y$ lie in the same orbit of Gu iff $\exists 6 \pi \in$ Ow s.t.

$$
\left(s_{1}, 1, \cdots, 1\right)=\left(t_{1}, 1, \cdots, 1\right)^{6 \pi}=\left(t_{1}{ }^{6}, 1, \cdots, 1\right)^{\pi}
$$

◯
if $1^{\pi}=1$, then $\left(s_{1}, 1, \cdots, 1\right)=\left(t_{1}{ }^{6}, 1, \cdots, 1\right)$ so $s_{1}=t_{1}{ }^{6}$
i.e. $s_{1}, t_{1}$ are conjugated by $6 \in \operatorname{Ant}(T)$.

If $1^{\pi}=k$, then $\left(s_{1}, 1, \cdots, 1\right)=\left(1, \cdots, 1, t_{1}^{6}\right)$

$$
\begin{equation*}
=\left((t, 6)^{-1}, \cdots,\left(t_{1}^{6}\right)^{-1}, 1\right) \in R . \tag{0}
\end{equation*}
$$

So $\mid \neq s_{1}=\left(t_{1}^{6}\right)^{-1}=\left(t^{-1}\right)^{6}$ and $k=2$ iotherwise $\left(t_{1}^{6}\right)^{-1}=1$. impossibe $3^{\circ}$ If $1^{\pi}=\left\{\neq S_{1}, k\right\}$, say $1^{\pi}=2$ wL06.

Then $\left(s_{1}, 1, \cdots, 1\right)=\left(1, t_{1}, 6,1, \cdots, 1\right) \in R$ so $s_{1}=1$ contradiction O Therefore $\left(s_{1},|\cdots|,\right)$ and $\left(t_{1}, 1, \cdots, 1\right)$ lie in the same orbit of 6w either $S_{1}=t_{1}^{6}$ or $k=2$ and $S_{1}=\left(t_{1}^{-1}\right)^{6}$
$\_\_\_\_$
Thus $\operatorname{rank}(G) \geqslant \#$ of orbits of $\operatorname{Aut}(T)$ on $\left\}^{2}, t^{-1}\right\}|t \in T| S_{1}$. and $t_{1}$ are $\geqslant 4$
-inverse - conj
under ant(T)

13 distinct primes divide order of the group. two elts of distinct order are not conj. nor inverse conj. in auto grapp). ◯
$\_\_\_\_$
$\_\_\_\_$
smaller 6 can be $\left.6=A s^{2}: \begin{array}{l}\langle\langle \rangle:\langle\pi\rangle) \\ \langle 6 \pi\rangle \\ \langle\pi\rangle\end{array}\right\}$ both tansitive. (has $S_{2}$ ). <Ti>

$$
\begin{aligned}
\Rightarrow \sigma_{w} & =S_{5} \times S_{2} \\
& =A_{1}+(T) \times S_{2}
\end{aligned}
$$

Def wreath product
$G=T 2 S_{k}=T^{k}: S_{k}$ wreath product.
where the semiproduct is defined as

$$
\left(t_{1}, t_{2}, \cdots, t_{k}\right)^{\pi}=\left(t_{1} \pi^{-1}, t_{2} \pi^{-1}, \cdots, t_{k} \pi^{-1}\right)
$$

or (equivalently) by multiplication of two elements

$$
\left(\left(s_{1}, \cdots, s_{k}\right) \pi_{1}\right)\left(\left(t_{r}, t_{2}, \cdots, t_{k}\right) \pi_{2}\right)
$$

$=\left(s_{1}, \cdots, s_{k}\right) \pi_{1}\left(t_{1}, \cdots, t_{k}\right) \pi_{1}^{-1} \pi_{1} \pi_{2}$
$=\left(S_{1} t_{1}^{\pi_{1}}, S_{2} t_{2} \pi_{1}, \cdots, S_{k} t_{k} \pi_{1}\right)$.

Def product action.
Let $T$ be a transitive permutation grap on $\Delta=\langle 0,1, \ldots, m-1$ For an integer $k \geq 2$, let $P \leq S_{k}$ be transitive, and let

$$
G=T 2 P \leq T 2 S_{k} \text { and } \Omega=\Delta^{k}
$$

such that $G$ acts on $\Omega$ naturally, namely

$$
\left(\delta_{1}, \cdots, \delta_{k}\right)^{\left(t_{1}, \cdots, t_{k}\right) \pi}=\left(\delta_{1}^{t_{1}^{\prime}}, \cdots, \delta_{k}^{t_{k}}\right)_{k}^{\pi}=\left(\delta_{1}^{t_{1} \pi^{-1}}, \cdots, \delta_{k}^{t_{1}}\right.
$$

In this way, $G$ is said to be auting on $\Omega=\Delta^{k}$ in product action or $G$ is a blow-up of $T$ on $\triangle$.

Fix a point $w_{0}=10, \cdots, 0$ ). For a point $w=\left(\delta_{1}, \cdots, \delta_{k}\right) \in \Omega$, define the weight of $\omega$ :

$$
\text { weight }(w)=|\langle i| \delta i \neq 0\} \mid \text {, }
$$

the number of nonzero entries of $W=\left(\delta_{1}, \cdots, \delta k\right)$. $T^{*}$ Then the action of each element $\left(t_{1}, \cdots, t_{k}\right) \pi \in N w_{0} \cdot S_{k} \cdot T_{k}^{k}$. preserves the weight of every point of eviry point of $\Omega 1$ swos. Thus $w_{1}$ not equivalent to $w_{2}$ under $G w_{0}$ if weight $\left(w_{1}\right) \neq$ weight 162

Lemma 3 (case product action).
Using the notation defined above. $(G=T 2 P \leqslant T 2 S k)$. We
(1) $\operatorname{rank}(6) \geqslant k+1$
(2) $\operatorname{rank}(6)=3$ iff $k=2$ and $T$ is 2-transitive on $\Delta$
pf :
(1) The weight function ranges $\{0,1, \cdots, k\}$.

So Gwo has at least $k+1$ orbits on $\Omega$.
(2) Assume $\operatorname{rank}(0)=3$. Then $k=2(k \geqslant 2)$. and $\Omega=\Delta x \Delta$ and $G=(T \times T): S_{2}$ with weight equal to 1 forming one $O$ orbit of Govo, so $\left.\left.S\left(\delta_{1}, 0\right) \mid \delta_{1} \in \Delta \backslash S_{0}\right\}\right\}$ is an or bit $O$ of $T_{0}$, i.e. $T$ is 2 -transitive on $\Delta$. Converse is easy $\square$

Week 12 Examples.
Let $V=F^{d}(d>1)$ and let $\sigma=\sigma L(V)$ acts on $V$ with orbits $503, V \backslash\{0\}:=\Omega$

- $G$ acting on $\Omega$ is transitive
(1) If $|F|>2$, then the action is imprimitive.
of: $\langle\langle V\rangle| v \in V \backslash$ SO3 \}form a block system $B$.
( $|\langle V\rangle|=|F|-1$ )
(2) If $|F|=2$, then the action is 2 -transitive.
of :
Let $\left(u_{1}, u_{2}\right),\left(v_{1}, v_{2}\right) \in \Omega \times \Omega, u_{1} \neq u_{2}, v_{1} \neq v_{2}$.
Then $u_{1}, u_{2}$ are l. so is $v_{1}$ and $v_{2}$. Then extend to bas

$$
\begin{array}{lll}
u_{1}, \cdots, & n d \\
v_{1}, \cdots, & v d
\end{array}
$$

$\exists g \in G L(v)$ s.t. $\quad u_{i}^{g}=v_{i}$
(3) If $|F|>2, G=G L(V)$ acts on $B$ transitively, with Kernel equal to $Z(6)$. Then $G / Z(0)=P G L(V)$ is 2-ransitive on $B$.
(4) PGL(V) on $B$ is not 3 -ransitive $\Leftrightarrow d>2$
pf:
F easy. choose one set of l.i. and one set of l.d.
⇒ if $d=2$.
Let $\left\langle u_{1}\right\rangle,\left\langle u_{2}\right\rangle,\left\langle u_{3}\right\rangle$ be pairwise different, $\left\langle v_{1}\right\rangle,\left\langle v_{2}\right\rangle,\left\langle v_{3}\right.$ be pairwise different
Then $u_{1}$ and $u_{2}$ are $l, T_{1}$. and $u_{3}=a u_{1}+b u_{2}$ with $a_{1} b \in F$ l
Similarly, $v_{1}$ and $v_{2}$ are $l .1$. and $v_{3}=c v_{1}+d v_{2}$ with $c_{1} d$ - FE veed to find $g \in G L\left(\frac{v_{c}}{2}\right)$ s.t. $\left(u_{1}, u_{2}\right)^{g}=\left(v_{1}, v_{2}\right) . u_{3}^{g}=\left(a u_{1}+b u_{2}\right)^{g}= \left.a v_{1}+b v_{2}\left(\begin{array}{l}\bar{a} \\ a\end{array} \mathrm{~g}\right) \mathrm{g}\right) \quad c v_{1}+d v_{2}$.
$C$ so $g: \quad \begin{aligned} & u_{1} \rightarrow c / a v_{1} \\ & u_{2} \rightarrow d / b v_{2}\end{aligned}$ then $u_{3} \rightarrow v_{3}$.

- Suppose $|F|>2$. Then $G L(V)$ acting on $V \backslash\{0\}=\Omega$ is transitive and imprimitive, of rank $=1+1+\left|F^{x}\right|-1=|F|(F$ finith pf: $\operatorname{rank}\left(6^{\Omega}\right)=$ \# of 6 - orbits on $\Omega \times \Omega$
$S(v, v) \mid v \in \Omega\}$.

$$
\begin{array}{lrl}
S(u, v) \mid\langle u\rangle=\langle v\rangle, u, v \in \Omega\} . & (u, \lambda u) & \rightarrow\left(v, \lambda^{\prime} v\right) \\
S(u, v) \mid\langle u\rangle \neq\langle v\rangle, u, v \in \Omega\} . & \text { then } & u^{g}=v \\
\lambda u^{g} & =\lambda v \\
\lambda & =\lambda^{\prime} \tag{0}
\end{array}
$$

⇒ partitions into $\left|F^{x}\right|-1$ orbits
Let $N=Z_{p}^{d}$ with $p$ prime, $d \geqslant 1$
Then $H=\operatorname{Aut}(N)=G \operatorname{Ld}(P)$
Let $G=N: H=H_{0} l(N)$.
let $\Omega=N$ and define an action of $G=Z_{p}^{d}: G L d(p)$ on $\Omega$ :

$$
\begin{equation*}
w^{x y}=(w+x)^{y}=w^{y}+x^{y} \in N \tag{O}
\end{equation*}
$$

✓ operation in $N$ is +

Claim: $G$ is an affine 2 -transitive permutation grapp on $\Omega$, Furthermore, if $p=2$, then $G$ is 3 -transitive (use (2) above) pf: Let $\left(n_{1}, n_{2}\right),\left(V_{1}, V_{2}\right) \in \Omega \times \Omega$
need $x y=g \in N_{x y}: H$ s.t.
◯

$$
\begin{gathered}
\left(u_{1}, u_{2}\right)^{x y}=\left(v_{1}, v_{2}\right) \\
\| \\
\left(\left(u_{1}+x\right)^{y},\left(u_{2}+x\right)^{y}\right) \\
\left(u_{1}^{y}+x_{1}^{y}, u_{2}^{\prime \prime}+x_{2}^{y}\right)
\end{gathered}
$$

$1^{\circ}$ can pust use 6 2-transitive on $\Omega \Leftrightarrow G_{w}$ is transitive on

Remark: easy to see $N$ is a minimal normal subgras. every pormal subgranp anterhs $N^{\text {Date }} N$ is trunsitive on $N$. so $G$ is quasi-primitive ⇒ affine.
$r 2^{\circ}$ y maps l.d. to l.d.; l.i. to l.i.
$C$ sboth l.i.: $\exists y \in G: H \quad J$
$C u_{1}, u_{2}$ l.i. \& $v_{1}, v_{2}$ l.d.: let $x=a u_{1}+b u_{2}$.

$$
\begin{aligned}
& u_{1}+x=(1+a) u_{1}+b u_{2} \\
& u_{2}+x=a u_{1}+(1+b) u_{2} \quad(\lambda \neq 1) . \\
& \Rightarrow \quad(1+a) u_{1}+b u_{2}=\lambda\left(a u_{1}+(1+b) u_{2}\right) \\
& \Rightarrow \quad 1+a=\lambda a \quad \Rightarrow \quad a=\frac{1}{\lambda-1} \\
& b=\lambda(1+b) \quad \Rightarrow \quad b=\frac{\lambda}{1-\lambda}
\end{aligned}
$$

$u_{1}, u_{2}$ l.d. \& $V_{1}, V_{2}$ l. $i$ : let $x$ l.i. of $u_{1}$ then $u_{1}+x$ and $u_{2}+x$ are l. 1 .
$u_{1}, u_{2}$ l.d. \& $v_{1}, v_{2}$ l.d.

$$
\begin{aligned}
& u_{1}=\lambda u_{2} \quad v_{1}=\lambda^{\prime} v_{2} \quad x, \lambda^{\prime} \neq 1 \\
& \text { let } x=y u_{2} \\
& \frac{x+u_{1}}{x+u_{2}} \frac{\lambda+y}{1+y}=x^{\prime} \Rightarrow\left(x^{\prime}-1\right) y=\lambda-\lambda \\
& y=\frac{\lambda-1}{\lambda-1}
\end{aligned}
$$

Def. $K$-transitive
$G$ is said to be $k$-transitive on $\Omega$. if for any $k$-tuples $\left(w_{1}, \ldots, w_{k}\right),\left(u_{1}, \ldots, u_{k}\right)$ where $w_{i}$ 's are distinct,
( $u_{i}$ 's are distinct, then $\exists g \in G$ s.t. $\left(w_{1}, \cdots, w k\right)^{g}=\left(u_{1} \cdots, u_{k}\right.$ i.e. $w_{i}{ }^{g}=u_{i}$
prop $G k$-transitive $\Leftrightarrow$ Gw $k-1$ transitive on $\Omega \backslash\{\omega\}$ and 6 transitive on $\Omega$.
if:

## ⇒ easy

$C \epsilon_{\lambda}\left(w_{1}, \cdots, w_{k}\right)^{g}=\left(u_{1}, \cdots, u_{k}\right)$ wi's aredistihet, $u_{1}^{-1}$ s appecuispho need to find $g \in G$ s.t.

Since 6 Transitive on $\Omega_{1}, \exists g_{11} g_{2} \in G$ s.t. $w_{1}^{g_{1}}=w$, $n_{1}^{g_{2}}=w$.
Then $\left(w_{1}, \ldots, w_{k}\right)^{g_{1}}=\left(w, w_{2}^{g_{1}}, \ldots, w_{k}^{g_{1}}\right)$
$\left(u_{1}, \cdots, u_{k}\right)^{g_{2}}=\left(w, u_{2}^{g_{2}}, \cdots, u_{k}^{g_{2}}\right)$
$\exists g \in G w$ s.t. $\left(w_{2}^{g_{1}}, \cdots, w_{k}^{g_{1}}\right)^{g}=\left(u_{2}^{g_{2}}, \ldots, u_{k} g_{2}\right)$ so $\left(w_{1}, \ldots, w_{k}\right)^{g_{1} g g_{2}^{-1}}=\left(u_{1}, \ldots, u_{k}\right)$.

Question: when is sharply 2-transitive? (smaller graup is no longer 2 -transitive).

Def sharply $k$-transitive.
$G$ is said to be, sharply $k$-transitive on $\Omega$ if for $\left(w_{1}, \ldots, w_{k}\right),\left(w_{1}^{\prime}, w_{2}^{\prime}, \ldots, w_{k^{\prime}}^{\prime}\right)$, there is exactly one elt $g \in \sigma$ s.t. $\left(w_{1}, \cdots, w_{k}\right)^{g}=\left(w_{1}{ }^{\prime}, \cdots, w_{k}{ }^{\prime}\right)$.

## Example

Let $F=\mathbb{F}_{q}$ where $q=p f$
Let $F^{+} \simeq \mathbb{Z}_{p}^{f}$

$$
F_{-x}^{x} \simeq \mathbb{Z}_{p} f-1=\langle\rho\rangle
$$

Then $F^{x}=\langle\rho\rangle$ acts on $F^{+}$naturally by the field multiplication:

$$
v^{\rho}=v \rho \in F^{+}
$$

$P$ is an automorphism of the grapp $F^{+}$, so $p \in G l f(p)$. and $G:=F^{+}:\langle p\rangle \leqslant \operatorname{Hol}\left(F^{+}\right)=Z_{p} f: G L_{f}(p)$.
Note that $\langle\rho\rangle=Z p^{f}-1$ is transitive on $F^{+1}\left\langle S_{0}\right\}$. i.e. for $u, v \in O$
$F^{+} \backslash\{0\}, u=\rho^{i}, v=\rho^{j}$ then $u=v \rho^{i-j}$ So 6 is 2 -transitive on $F^{+}$.
Contrumbers: $\#\left(w_{1}, w_{2}\right)$ with $w_{1} \neq w_{2} \in F^{+}=p^{f}\left(p^{f}-1\right)=|G|$.

## Week 13. Hall subgrap

C Def Hall subgroup
Let $G$ be a finite graup of order $|G|=p_{1}^{f_{1}} p_{2} f_{2} \ldots p_{t} f_{t}$ where pi's are the prime divisors of $|G|$. Let

$$
\pi(6)(=\pi \| G 1))=\left\{p_{1}, \ldots, p_{t}\right\} .
$$

For a subset $\pi \subset \pi(G)$, a subgrapp $H \leqslant G$ is called a Hall $\pi$-subgrap of 6 if $\pi(H)=\pi$ and $\operatorname{gcd}(|H|,|G| / \mid H)=1$ (1) If $|\pi|=1$, then a Hall $\pi$-subgrapp is a Sylow subgrapp.
(2) Hall subgraps do not necessarily exist
(3) Hall $\pi$-subgraups are not necessarily conjugate.

## Notation

$6 \pi \rightarrow$ Hall $\pi$-subgrap $\Rightarrow \pi$ in fact takes $\pi \cap \pi 6$ )
$G p \rightarrow$ if $z=\Delta p$.
$\pi \rightarrow \pi(6) \backslash \pi$.
eg. $G=A_{5} . \quad|G|=2^{2} \cdot 3 \cdot 5$.

$$
\begin{aligned}
& G_{\{2,7\}}=2^{2} 7^{0} \\
& G_{\{2,3\}}=A_{4} \\
& G_{5}=C_{5}=G_{\left\{2,33^{\prime}\right.}
\end{aligned}
$$

## example

(1) $G=A_{5} .|G|=60$.

$$
\begin{array}{rlr}
\pi= & \{2,3\} & \\
& \{3,5\} & x \\
& \{2,5\} & x
\end{array}
$$

Pf: If $H$ is a Hall $\{2,5\}$-subgrap. Then $[G: H]$ is of size 3 , then $G \leqslant \operatorname{Sym}([G: H]) \simeq S_{3}$ which is impossible.
$\forall G=A s$ is simple, hence $G \rightarrow \operatorname{Sym}([\bar{G}: H]) \mathrm{ker}=c$
similarly for $\{3,5\}$.
(2) $G=P S L_{2}$ (11) is simple. $|G|=5 \cdot 11 \cdot 12$. has subgrap $H_{1} K$ s.t. $H \simeq A 4, K \simeq D_{12}$ which are Hall $\{2,3\}$ - subgraups of 6 . They are not isomouphic $\Rightarrow$ not conjugated.

Thm (Hall thm) Solvable ⇔ each Hall $\pi$-subgps exists. Let $G$ be a finite grapp with $|G|=p_{1} f 1 \ldots p_{t} f t$ with $p_{1}, \ldots, p_{t}$ being distinct primes. Then $G$ is solvable ⇔ $G$ has Hall $\pi$-subgraps for each subset $\pi \subset \pi(6)$ pf:
When $\pi(0)=1, G$ is a p-grap. Then $G$ is solvable (nontrivial conter $+N \Delta G, N$ and $G / N$ both solvable $\Rightarrow G$ solvablel.
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-64.jpg?height=104&width=1570&top_left_y=1247&top_left_x=47)
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-64.jpg?height=107&width=1610&top_left_y=1326&top_left_x=54)

If $|\pi(6)|=2$, then $G$ is solvalde by Burnside's " $p^{a} q$ "-then Assume $t \geqslant 3$. Let $H_{1}=H_{p_{i}}$ be a Hall spis $i^{\prime}$ - subgp of 6. for $1 \leqslant 1 \leqslant t$. Then $H_{1}$ is solvable as $\left|\pi\left(H_{i}\right)\right|=t-1$

Let $p\left|\left|H_{1}\right|\right.$, i.e. $p=p j$ for some $j \in\{2, \cdots, t\}$. s.t. $O_{p}\left(H_{1}\right) \neq 1$.
(let $x$ be a grap. Then $\underline{P(x)}$ satisfies, characteristic $O$
(1) $O_{p}(x) \& X \quad \forall$ maximal normal $p$-subgrub sinep.
(2) $O_{p}(x)$ is a $p$-grap. the product of all normal p-subges of
(3) $X / O_{p}(x)$ has no normal $p$-subgraps $\left(O_{p}(x / O p(x))=1\right)=1$ otherwis
$\Leftrightarrow O_{p}(x)$ is the intersection of all sylow $p$-subgroups) Esuch a $p$ exists since a minimal normal subgraup of a soluablenornal BO group is an elementary abelian $p$-grapp ( $Z_{p}^{d}$ for some $p$ andgunde, sum, bgow thenpust is contained in $O p\left(H_{1}\right)$. ).

Then $p$ divides $\left|H_{2}\right|$ or $\left|H_{3}\right|$, say $p\left|\left|H_{2}\right|\right.$. Let $P$ be a Sylow $p$-subgp of $H_{2}$. Then $g$ is a sylow $p$-subgp of $G$ and $O_{p}\left(H_{1}\right) \leqslant p^{g} \leqslant H_{2} g$ for some $g$ by sylow thm. Further, we have $G_{g}=H_{1} H_{2} g \left(H_{1} H_{2}{ }^{g} \leq G . \quad\left|H_{1} H_{2}^{g}\right|=\frac{\left|H_{1}\right| H_{2} g \mid}{\left|H_{1} \cap H_{2} g\right|}\right.$

$$
\geqslant \frac{\left(p_{2}^{f_{2}} p_{3}{ }^{f_{3}} \cdots p_{t}^{f_{t}}\right)\left(p_{1}^{f_{1}} p_{3}^{f_{3}} \cdots p_{t}^{f^{\prime}}\right.}{p_{3}^{f_{3}} \cdots p_{t}^{f_{t}}}
$$

Let $N=\left\langle O_{p}\left(H_{1}\right)^{x} \mid x \in \sigma\right\rangle$ the normal closure of $O_{p}\left(H_{1}\right)$ in G. Then $N \triangleleft G$ and $N \leq H_{2}^{g}$ as $\forall x \in G, x=x_{1} x_{2}$ for some $x_{1} \in H_{1}, x_{2} \in H_{2}^{g}$, then $O_{p}\left(H_{1}\right)^{x}=O_{p}\left(H_{1}\right)^{x_{1} x_{2}}= O p\left(H_{1}\right)^{x_{2}} \leq H_{2}{ }^{g}$. Thus $N$ is a proper normal subgrapp of 6. For a Hall $\pi$-subgrapt $H$ \& $G, H \cap N$ is a Hall $\pi$-subg of $N$ and $H / H \cap N \simeq H N / N$ is a Hall $\pi$-subgp of $G / 1 \left(|N H|_{\lambda}=|H|_{\lambda} \Rightarrow|N \cap H|_{\lambda}=\frac{|H|_{\lambda}|N|_{\lambda}}{|H N| \pi}=|N|_{\lambda}\right)$
By induction, $N$ and $G / N$ are both solvable, so $G$ is solvable
Let $\sigma$ be solvable. Let $M$ min $^{\text {G }}$. Then $M=Z_{p}{ }^{d}$ for some prime $p$ and $d \geqslant 1$ and $\bar{G}=G / M$ has order properly dividin 161. Then $\bar{G}$ has Hall 6 -subgraps for all $6<\pi(\bar{G})$ (By $\bar{G}$ solvable and use induction).
( Take a subset $\pi \subset \pi(6)$. Let $\bar{H}$ be a Hall $\pi$-subgrap of $\vec{G}$.
If $p \in \pi_{1}$, then the full image of $\bar{H}$ under $G \rightarrow \bar{G}$ is $M \cdot \bar{H}$ which is a Hall $\pi$-subgranp of $G$.
If $P \notin \pi$, aim to find a subgrap of order $|H|$ of 6 . Now $(p, \bar{H})=1$. Let $X:=M \cdot \bar{H}$ be the full preimage of $F$ under $6 \rightarrow \overline{6}$. If $X<6$ then by induction $X$ has a Hall $\pi$-subgrap $H$ which is isomophic to $\bar{H}$ and is a kepkafto

$$
\left(X=M H^{\prime}=M \cdot \bar{H}\right) .
$$

$\pi$-subgrap of $G$. Then $\bar{G}=\bar{H}$
Assume $X=G$. $r_{\text {Let }} \bar{Q}=O_{q}(\bar{G}) \neq 1$ for some $q \in \pi(\bar{G})$. Then the full preimage of $\bar{Q}$ under: $\bar{G} \rightarrow \bar{G}$ has the form $M \cdot \bar{Q}=M: Q \Delta G$ for some sylow $q$-subgroup $Q \simeq \bar{Q}$ of $M \cdot \bar{Q}$. (full preinage of a normal subgroup is a normal subgrapp).
DSuppose $C_{M}(\theta) \neq 1$
Then $C_{M}(\theta)=Z(M: Q) p$ $\_\_\_\_$
$(C M(\theta) \in Z(M: \theta)$ since $M$ abelian, $P$ is a characteristic

$$
\underset{\downarrow}{P} \times Q_{1} \longrightarrow \text { sylow } g \text {-subgp. }
$$

Sylow P-subge
subgroup of $P \times \theta_{1}=Z(M: \theta), Z(M: \theta)$ is a characteristic subgrap of $M: Q \triangleleft G$, so $P \triangleleft G$. As $P \leq M \underset{\min }{\triangle} G, P=M$ and thus $M: Q=M \times Q$.) ◯
Now $\theta$ is characteristic of $M \times \theta=M: \theta \& G$ we have
$Q \Delta G$. Let $\bar{K}$ be a $\pi$-subgp of $6 / Q$. Then the full preinage of $\bar{K}$ under $G \rightarrow G / Q$ is a Hall $\pi$-subgrap $\_\_\_\_$ of 6 (case $p \in \pi$ ).
(2) Suppose $C_{M}(\theta)=1$.

Note that $N_{M}(\theta)=C_{M}(\theta)$
$\_\_\_\_$
(let $x \in M$ s.t. $x \in N M(\theta)$, then $x y x^{-1} y^{-1} \in M \cap \theta=113$ ).
$\_\_\_\_$

Claim: $N_{G}(\theta) \simeq N \bar{G}(\bar{\theta})=\bar{G}$ (as $\bar{\theta} \triangleleft \bar{G}$ ).

Let $\bar{g} \in \bar{G}=N_{\bar{G}}(\bar{G})$ and let $g$ be a preimage of $\bar{g}$ in $G \rightarrow \bar{G}$
$\_\_\_\_$
Then $\exists$ a unique $x \in M$ s.t. $g x \in N_{G}(\theta)$
$\left(\bar{g}^{-1} \bar{Q} \bar{g}=\bar{Q} \Rightarrow g^{-1}(M: \theta) g=M: \theta=M: g^{-1} \dot{Q} g\right.$.
◯
i.e. $g^{-1} \theta g$ is a Sylow $q$-subgranp of $M: Q$ So $\exists x \in M$ $\_\_\_\_$
s.t. $x^{-1}\left(g^{-1} \theta g\right) x=Q^{g x}=Q$ by sylow thm . i.e. $g x \in N G(\theta)$ $\_\_\_\_$
Suppose $g x^{\prime} \in N G(Q)$ with $x^{\prime} \in M$. then $x^{-1} x^{\prime}=(g x)^{-1} g x^{\prime} \in N G C O Q$. $\square$
since $\left.\left.x^{-1} x^{\prime} \in M, \quad x^{-1} x^{\prime} \in N_{G}(\theta) \cap M=N_{M}(\theta)=31\right) \Rightarrow x=x^{\prime}\right)$ Define $\rho: N_{\bar{G}}(\bar{\theta}) \rightarrow N_{G}(\theta)$

$$
\bar{g} \mapsto g x
$$

Suppose $g_{1} x_{1}=g_{2} x_{2}$ for some $x_{1}, x_{2} \in M$.
Then $g_{2}^{-1} g_{1}=x_{2} x_{1}^{-1} \in M$. so $\overline{g_{2}^{-1} g_{1}}=\overline{x_{2} x_{1}^{-1}}=T$ so $\bar{g}_{2}=\bar{g}_{1}$ i.e. $\varphi$ is injective.
so $|N \bar{G}(\bar{\theta})| \leqslant\left|N_{G}(\theta)\right|$.
Then " $=$ " must hold. and we are done (if " $<$ ", then $M N_{B}(\theta) / M \leqslant G / M=\bar{G}$ while $M N_{G}(\theta) / M=N_{G}(\theta) / M \cap N_{G}(\theta)=N_{G}(\theta) / N M(\theta)= N_{6}(\theta)$. contradiction). $\square$

Remark. Morever, for G solvable, Hall $\pi$-subgraups of 6 are conjugated for any $\pi<\pi(6)$.
pf: Let $G$ be solvable $\exists M<G$ s.t. $|G / M|=\dot{p}$ for some prime $P$
(let $N \underset{\text { min }}{\delta} 6$. Then $G=G / N$ has a subgranp $I$ s.t. $|\bar{G} / \tau|=$ by induction. Let $L$ be the full premage of $\bar{L}$ under $G \rightarrow \bar{G}$. Then $\left.\frac{|G|}{|L|}=\frac{\frac{|G|}{|L|}}{\frac{1 L \mid}{|N|}}=\frac{\sqrt{G} \mid}{|L|}=p\right\rangle$.
Let $H, k$ be Hall $\pi$-subgraps of 6 . If $p \notin \pi$, then $H, K \leq M$. $\int_{H} 0, H, K$ are conjugated by induction.
$\left(|H \cap M|_{\pi}=\frac{|H|_{\pi}|M|_{\pi}}{|H M|_{\pi}}=|H|_{\pi}\right.$.)
Assume $p \in \Pi$. Then $H / H \cap M \simeq \mathbb{Z}_{p} \simeq K / K \cap M$ and $H \cap M, K \cap M$ are Hall $\pi$-subgraps of $M$. By induction, $(H \cap M)^{X}=K \cap M$ for some $X \in M$. Now $H^{X}$, $K$ are Hall $Z$-subgraps of $G$. with $K=\langle K \cap M, g\rangle$ soffero some $g^{P} \in K \cap M$.

## Week 15 nilpotent graps

## Thm Frattini argument

Let $\sigma$ be finite and $N \Delta G$ with
Then for $P \in \operatorname{Syl}_{p}(N)$ we have $G=N_{G}(P) N$.
Pf:
By Sylow thm, $N$ is transitive on Sylp (N) by conjugatio Since $N \triangleleft G, G$ induces an action on Sylp(N).
(Na 6, $\forall Q_{\in} S y l_{p}\left(N_{3}\right), Q^{g} \in S y l p(N)$. As $N$ is transitive on Sylp (N), we have $G=N N 6(P)$

$$
\text { Transisitive } d G p \text {. }
$$

## Nilpotent graps

(1) Let 6 be a p-grap, then $z(6) \neq 1 . z(G / z(6)) \neq 1$. (if $G$ no abelian). Let $K$ be the full preimage of $z(G / z(G))$ under $G \rightarrow G / Z(G)$. Then $Z(6) \varangle K \triangle G$. Continue in this way we geta normal series.
(2) Let $\sigma=P \times Q$, where $P$ is a $P$-granp, $\theta$ is a q-grap, $P \cdot q$ distinat primes. Then $z(G) \neq 1$ and $G / z(G)=\bar{P} \times \bar{Q}$

Def nilpotent graup
A grapp 6 is called nilpotent if it has a chain of subgp:

$$
G=K_{1}>K_{2}>K_{3}>\cdots>K_{s+1}=1
$$

such that $k_{i} \triangleleft G$ and $k_{i} / k_{i+1} \leqslant z\left(G / k_{i+1}\right)$.
(such a series is called a central series).
example not nilpotent graps.
(1) $A_{4}=C_{2}^{2}: C_{3}$ only one normal subgrapp $C_{2}^{2}$

$$
A_{\|_{1}}>C_{\|_{2}}^{2}>1_{k_{2}} \quad C_{2}^{2}=k_{2}=k_{2} / k_{3} \leqslant z\left(A_{4}\right)_{\substack{\text { kor } \\ \text { contradiction }}}
$$

(2) $S_{4}$ has normal subgranps $\dot{A}_{4}, C_{2}^{2}$, Then $\quad S_{4}>A_{4}>C_{2}^{2}>1$ not possible since $C_{2}^{2} \neq Z\left(S_{4}\right)$.
(3) Dzp $p \geqslant 3$ prime.

It has only one normall subgreenp $\langle a\rangle$ if $D_{2 p}=\langle a, b| a^{p}=b^{2}=1, a^{5}=0$
Then $D_{2 p}>\langle a\rangle>1$
But $\langle a\rangle \neq z\left(D_{2 p}\right)$.

Thm equivalence definitions of being a nilpotent grap.
11) $G$ is nilpotent
(2) If $H_{\neq} G$, then $N_{G}^{-}(H)$ 干 $H$.
(3) each maximal subgrap $M$ of $G$ is normal and is of prime index (i.e. $G / M \simeq Z p$ for sime $p$ ).
(4). $G$ is a direct product of its Sylow subgraps i.e. each sylow subgrap is normal.

## Example

(1) Abelian graups are nipotent graups.
(2) Subgraps and factor graps of nilpotent graps are nilpotent.
(3) $G=A_{4}=C_{2}^{2}: C_{3} . C_{3}$ is maximal.

But it is not prime nor of prime index.
of of then:
(1) ⇒ (2).
$G$ has a chain of subgraps

$$
G=k_{1}>k_{2}>\ldots>k_{5}+1=1
$$

let, $H \leqslant 6$. Then $\exists i$ such that $k_{i+1} \leqslant H$ and $k_{i} \notin H$.

Then $H / K_{i+1}$ and $Z\left(G / K_{i+1}\right)$ centralizes each other. As $k_{i} / k_{i+1} \leq z\left(6 / k_{i+1}\right)$, for any $h \in H, y \in k_{i}$, $h y h^{-1} y^{-1} \in K_{i+1} \subset H$
Hence $y h^{-1} y^{-1} \in h^{-1} H=H$.
i.e. $y \in N \sigma(H)$ thus $k_{i} \in N_{\sigma}(H)$
Thus $N G(H) \geqslant H K_{i}>H$ as in (2).
(2) ⇒ (3). → notation of maximal grap.
let $M(\delta G$, then $M \approx N G(M)=G$. so $M \triangleleft G$.
Then $G / M$ does not have a nonidentity proper subgrap as $M$ is maximal, so $|G / M|$ is prime as in (3).
(3) ⇒ (4).

Let $P \in \operatorname{Sylp}(G)$. Then $P<G$ and $P<N_{G}(P) \leqslant G$ Suppose $N G(P) \neq G$. Then $\exists M$ s.t. $N_{G}(P)<M<G$. Then $M \& G$ and by Frattini argument, $G=N G(P) M=M$. contradiction. hence $P \triangle N_{G}(P)=G$ as in (4).
(4) ⇒ (1)

Let $|G|=p_{1}^{e_{1}} p_{2}^{e_{2}} \ldots p_{t}{ }^{e_{t}}$ with $p_{1}, \ldots, p_{t}$ be distinct primes. Let $P_{i} \in S y l P_{i}(6)$. Then $P_{i} \& G$ and $G=P_{i} x-x P_{t}$ let $L_{1}=2(6)$. Then $L_{1} \neq 1$ and $\bar{G}=G / L_{1}$ is again a direct sum of its Sylow subgrarps.
Let $\bar{\tau}_{2}=Z(\bar{G})$. and let $L_{2}$ be the full primage of $\bar{L}_{2}$ under $G \rightarrow \bar{G}$. Then $L_{2} \Delta G$ as $\bar{L}_{2} \Delta \bar{G}$ and $L_{2} / L_{1}=Z\left(G / L_{1}\right.$. Let $\bar{L}_{3}=2\left(6 / L_{2}\right)$ and let $L_{3}=L_{2} \cdot \bar{L}_{3}$ be the full premage of $\tau_{3}$ under $6 \rightarrow G / L_{2}$. Then $L_{3} \triangle 6$ and $L_{3} / L_{2}=Z\left(6 / L_{2}\right)$
Repeating this process produce a chain of subgraps

$$
1<L_{1}<L_{2}<L_{3}<\cdots<L_{S}=6 .
$$

such that $L_{i} \triangleleft G$ and $L_{i} / L_{i-1}=Z\left(G / L_{i-1}\right)$ as in (1). $D$.

## Frattini subgrap \& nilpotent grap

Def Frattini subgrup.
Let 6 be a finite grap. Then $G$ must have many maximal subgraps say $\operatorname{Max} G=5 M<G 3$. Then the intersection of all maximal subgraps is called the Frattini subgrup. of $G$, denoted by $\Phi(G)$.
corollary (to be obtained)
(1) $\Phi(6)$ is nilpotent
(2) $G$ nilpotent $\Leftrightarrow G / \Phi(G)$ nilpotent
(3) (Whelandt) $G$ nilpotent $\Leftrightarrow G^{\prime} \leqslant \Phi(G)$.
(4) If $G$ is a p-grap, then $G / \Phi(6)$ is elementary abelian

## Observation

(1) $\Phi(6)$ is a characteristic subgrapp of $G$.
(2) If $N \varangle G$, then $\Phi(N) \& G$. (by (1)).

Def non-generator

An elt $x$ of 6 is called a non-generator if $\langle x, 5\rangle=6$ then $\langle s\rangle=6$ for any. $S \subset C$.

## Example

(1) $G=\langle a\rangle=C p^{2}$ Then each elt of $\left\langle a^{p}\right\rangle$ is a nongenerator
(2) Q8 has nongenerators of order 1 or 2

Compus
use lemma below.
(3) $D_{2 n}$
(4) A nonabelian simple graup has no nonidentity nongenerator ett In fact, $\forall a, \exists b$ s.t. $\langle a, b\rangle=0$.

Lemma characterization of $\Phi(6)$.
$\Phi(6)$ consists of all non-generator of $G$.
pf:
Let $x \in \Phi(6)$. Suppose $\langle x, S\rangle=G$ s.t. $\langle S\rangle \neq G$.
Then $\exists M<G$ s.t. $<s><M<G$. Then $x \in \Phi(G) \leq M$ so $G=\langle x, S\rangle \subseteq M$, not possible. Hence $x$ is a nongenerator Let $x \in G$ be a nongenerator. Suppose $M<-G$ and $x \notin M$ Then $\langle x, M\rangle=G$, not possible as $M<G$. So $x \in M$

Thm (Guschutz)
Let $N \triangle G$ s.t. $N \leq \Phi(G)$. Then $G / N$ is nipoten $\Leftrightarrow G$ is nilpotent.
pf: It suffices to prove ⇒
let: $P \in S y l p(G)$. We will show $P \triangle G$.
Then $P N / N \in S y l p(G / N)$ and $P N / N \triangle G / N$ Hence $P N \triangle G$ By Frattini argument $G=P N N_{G}(P) \xlongequal{N \leq \phi(6)} P N_{6}(P) =N_{G}(P)$ so $P \triangle G$, and thus $G$ is nilpotent.

## of of corollary:

(1) It suffices to prove any. Sylow $p$-subgp $P$ if $\Phi(6)$ is normal in $\Phi$

By．Frattini argument，$G=\Phi(G) N_{G}(P)$ ．If $N_{G}(P) \neq G, \exists M$ s．t．$N_{G}(P)< M<c \quad$ But $\phi(6) \leqslant M$ ，so $G \leqslant M$ ，contradiation．Hence $P<G$ ．

74
◯
（2）Take $N=\$(6)$ in the thm．
◯
（3）Suppose 6 is nilpotent．Then for any $M<G, M<G$
◯ and $G / M \simeq C p$ is abelian．Thus $G^{\prime} \leqslant M$ and $\Phi(6) \geqslant G^{\prime}$ （4）．By（3），$G / \Phi(6)$ is abelian．Merever，$x^{p} \in \Phi(6) \quad \forall x \in G$ Indeed，Let $M<6$ ，if $x \in M_{1}$ ，we are done．If $\dot{x} \notin M$ ，
$\_\_\_\_$ then Since $G / M \simeq C p$ ，we have $x^{p} \in M$ ．Then $\forall \bar{g} \in G / \Phi(6), \bar{g}^{P}=1$ ．Hence $G / \Phi(6)$ is elementary abelian．
◯

## example

Let $G=C_{p}^{2}: C_{q}=\langle a\rangle:\langle b\rangle$ ．where $p \neq q$ primes．
◯
◯
$\_\_\_\_$
$\_\_\_\_$
Then $a^{p} b=b a^{p} \Leftrightarrow a b=b a \Leftrightarrow G / k a^{p}>$ is abelian．
◯
In particular，$G B$ abelian $\Leftrightarrow G /\left\langle a^{p}\right\rangle \simeq C_{p}: C_{q}$ abelian．
◯

## Def Fitting subgrap

The product of all normal hilpotent subgraps of 6 is called the Fitting subgrap of $G$ ，denoted by $F(6)$ ．
$\_\_\_\_$ $\Leftrightarrow \Phi(6)<F(6))$ ．

## Thm

Let $G$ be solvable，and $F=F(G) .|G|=p_{1}^{e_{1}} p_{2} e_{2} \cdots p_{t} e_{t}$ ．Then $($ （1）$F=O p t(6) \times \cdots \times O p+(6) \quad(O p, 16) \times \cdots \times O p+(6)$ characterition （2） $\operatorname{Co}(F) \leqslant F$ i．e．$F$ is self－centralizing．（自中心化）．subg of $\left(\Rightarrow G / C_{G}(F) \leqslant \operatorname{Ar}+(F)\right)$ ．

## Recall $O p(6)$ ．

Let $p||6|$ Then
◯
◯
Campus

$$
O_{p}(6)=\left\{\begin{array}{l}
\text { the largest normal } p \text {-subyp of } 6 . \\
\text { the product of all normal } p \text {-subgraps of } 6 \\
\bigcap_{p \in S y l p(6)} p
\end{array}\right.
$$

(1) $O_{p}(6)$ char $G(O p(6)$ is a characteristic subgrap) and $\bar{G}: \sigma / O p(\sigma)$ is s.t. $O p(\bar{G})=1$.
(2) If $q \neq p$, then $\left\langle O_{p}(6), O_{q}(6)\right\rangle=O_{p}(6) \times O_{q}(6)$ charl
of of thm:
(1) Let $N$ be a normal nilpotent subgrap of $G$.

Then $N=\theta_{1} \times \theta_{2} \times \cdots \times \theta_{t}$ where $\theta_{i} \in S y l_{p_{i}}(N)$.
Now $\theta_{i}$ char $N \triangle G$, so $\theta_{i} \& G$ and thus
$\theta_{i} \leqslant O_{p i}(6)$. Hence $N \leqslant O_{p i}(6) \times \cdots \times O_{p t}(6)$.
Since $O_{p 1}(6) \times \cdots \times O_{p t}(6)$ is a normal nilpotent subgra of 6 , we have $F=O p_{1}(6) \times \cdots \times O p i(6)$.
(2). Let $C=\operatorname{Co}(F)$. Then $C \cap F \leqslant Z(F)$. Since $F$ char $G$, so are $C, C F$ and $C \cap F$. Let $W=C \cap F$.
$\bar{G}:=G / W$. Suppose $\bar{C} \neq 1$. Then there exists p.t.
$O_{p}(\bar{C}) \neq 1$. and $O_{p}(\bar{C})$ char $\bar{C}$ char $\bar{G}$. Then the full preimage of $O p(\bar{C}) y:=W \cdot O p(\bar{C}) \leqslant C$ char $G$. Let $P \in$ Sylp $(y)$. Then $w_{p} \leqslant P \leqslant C$. By def of $W$ and $C$, $P$ centralizes $W_{P}^{\prime}$, so $y=W_{P}^{\prime} \times P$.
Then $P$ char $y$ char $G$ and $P \& G$ so that $w_{P}<P \leqslant F$, a contradiction Thus $O P(\bar{C})=1$ and $c \leqslant Z(F) \leqslant F$. as $P \leq C \cap F$.

## example

Let $G$ be such that each sylow subgrap is cyclic. Then $G$ is solvable. Further, $G$ is metacyclic, i.e. $\exists C \nabla G$ is yolic and $G / C$ is cyclic.
pf: Let $F=F(G)$. Then $F$ is cyclic and $G / C_{G}(F) \lesssim$ Aut $(F)$ is abelian. Since $F$ is abelian, $C_{G}(F)=F$ and $G / F$ is abelian. Since Sylow. Hence $G$ is solvable. subges of $G / F$ are cyclic, $G / F$ is cyclic.
:HW) (1) $G$ is as in example above.
![](https://cdn.mathpix.com/cropped/c1d1dc6f-ba9f-4031-942a-b4591b7a5de5-76.jpg?height=202&width=1664&top_left_y=918&top_left_x=102) (2) Let $G$ be solvable and $|G|=4 m$ where $m$ is "dd" and) square-free. Characterize G.
(3) example before
(4) solvable
(5) (1)
$|F|_{p}=|6|_{p}$.
other p go to $\pi^{\prime}$

